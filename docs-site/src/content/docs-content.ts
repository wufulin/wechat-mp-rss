// Auto-generated by scripts/build-docs.mjs
export const docsContent = {
  "docs/workflows": `# 系统核心流程

本文按步骤串起 WeRSS 的主干数据流，便于对照代码与配置。

---

## 1. 公众号订阅 -> 文章采集 -> 文章入库

本流程面向私有自用场景。请仅在有权访问、保存和使用相关内容的范围内启用采集能力，避免公开批量抓取、缓存或再分发第三方内容。

**入口：**
- Web UI：\`/subscriptions\` 页面添加公众号订阅
- API：\`POST /api/v1/wx/subscriptions\`

**采集方式（配置项 \`gather.content_mode\`）：**
- \`web\`：使用 Playwright 浏览器驱动（\`driver/wxarticle.py\`）抓取
- \`api\`：使用 \`WxGather\` 类（\`core/wx/\`）调用微信 API 提取
- \`app\`：App 采集模式

**定时采集：**
- 由 \`jobs/mps.py\` 中的定时任务触发
- 调度器 \`TaskScheduler\` 按 cron 表达式执行 \`start_job()\`
- 采集时检查微信 Session 是否有效（\`check_session_valid()\`）

**文章入库：**
- 文章存入 \`Article\` 表（\`core/models/article.py\`）
- 关键字段：\`id\`, \`mp_id\`, \`title\`, \`url\`, \`publish_time\`, \`content\`, \`digest\`, \`pic_url\`
- 状态：\`status = DATA_STATUS.ACTIVE\`（1=正常，2=禁用，3=已删除）

---

## 2. 文章 -> 标签提取 -> 标签管理 / 标签筛选

**标签来源有两种：**

### 2a. AI 自动提取
- 调用 \`core/tag_extractor.py\` 的 \`TagExtractor.extract_with_ai()\`
- 通过 OpenAI 兼容 API（配置项 \`openai.api_key\` / \`openai.base_url\` / \`openai.model\`）
- Prompt 模板见 \`core/tag_extractor.py\` 中的 \`DEFAULT_TAG_EXTRACT_PROMPT_TEMPLATE\`
- 默认每次提取 5 个标签（配置项 \`article_tag.max_tags\`）
- 提取时优先匹配用户自定义标签（\`is_custom=True\` 的标签）

### 2b. 手动创建标签
- 通过 API：\`POST /api/v1/wx/tags\`
- 或 Web UI：\`/tags\` 页面

**标签管理：**
- API：\`GET /api/v1/wx/tags\`（分页列表，含近 3 天文章数统计）
- \`PUT /api/v1/wx/tags/{tag_id}\` 更新标签
- \`DELETE /api/v1/wx/tags?tag_ids=id1,id2\` 批量删除

**标签筛选（filtered tag）：**
- 标签通过 \`is_filter\` 字段区分是否用于筛选
- 文章列表 API 支持按标签筛选（\`tag_id\` / \`tag_ids\` 参数）

---

## 3. 标签 -> 标签聚类

**功能：**
- 将语义相近的标签聚合为标签聚类（Tag Cluster）
- 位于 \`core/tag_cluster.py\` 的 \`rebuild_tag_clusters()\` 函数

**算法：**
- 基于 embedding 相似度 + 字符 n-gram 相似度的加权组合
- 建图后稀疏化（每个节点保留 top-k 边），再用连通分量生成簇
- 配置项：\`TAG_CLUSTER_SIMILARITY_THRESHOLD\`（默认 0.78）、\`TAG_CLUSTER_MAX_SIMILAR_TAGS\`、\`TAG_CLUSTER_GRAPH_TOP_K\`

**与标签本身的区别：**
- 标签（Tag）= 文章的关键词
- 标签聚类（Tag Cluster）= 多个相近标签的聚合

**入口：**
- API：\`POST /api/v1/wx/tag-clusters/rebuild\` 触发重建
- 定时任务：默认每天执行

---

## 4. 最近文章 -> 热点发现

**功能：**
- 从最近 N 天的文章中发现科技热点主题
- 位于 \`core/hot_topics/service.py\` 的 \`run_discovery()\`

**时间窗口：**
- 默认 3 天（配置项 \`hot_topics.window_days\`）
- 可通过 \`hot_topics.cron\` 配置定时任务触发时间

**数据流：**
1. 从 \`Article\` 表拉取近 N 天文章（带标签）
2. 调用 LLM 对文章进行事件级聚类
3. 解析 LLM 返回结果，存入 \`HotTopic\` 表
4. 关联文章到 \`HotTopicArticle\` 表

**与标签聚类的区别：**
- 标签聚类 = **标签的聚合**（embedding 相似）
- 热点发现 = **文章的聚合**（内容/时间相关）

**入口：**
- Web UI：\`/hot-topics\` 页面
- API：\`GET /api/v1/wx/hot-topics/recent\` 获取最近结果
- API：\`POST /api/v1/wx/hot-topics/rebuild\` 手动触发

---

## 5. 文章 -> RSS 输出

**功能：**
- 将文章摘要输出为标准 RSS 2.0 订阅源；如需全文输出，应先确认内容授权、使用范围和分发对象

**实现：**
- \`core/rss.py\` 生成 RSS XML
- API：\`GET /rss/{feed_id}.xml\`

**数据来源：**
- 对应一个 \`subscription\`（订阅）
- 关联 \`feed\` 表（\`core/models/feed.py\`）

---

## 6. 文章 -> 消息任务 -> Webhook 通知

**功能：**
- 定时汇总公众号文章，通过 Webhook 发送通知

**实现：**
- \`jobs/taskmsg.py\` 获取消息任务
- \`jobs/mps.py\` 的 \`do_job()\` / \`do_job_all_feeds()\` 执行采集和汇总
- \`jobs/webhook.py\` 的 \`web_hook()\` 发送 Webhook 请求
- \`core/lax.py\` 的 \`TemplateParser\` 渲染消息模板

**通知渠道：**
- 支持钉钉、企业微信、飞书、Webhook

**入口：**
- Web UI：\`/message-tasks\` 页面
- API：\`POST /api/v1/wx/message-tasks\`

---

## 7. 文章 -> 导出 / 图片转存

**导出功能：**
- 支持 PDF、Markdown 格式，建议仅用于个人归档或已授权内容
- API：\`POST /api/v1/wx/export\`

**图片转存：**
- 使用 MinIO 对象存储（配置项 \`minio.\`），默认不建议转存第三方图片；确有授权或私有归档需求时再开启
- \`core/storage/\` 实现转存逻辑

**入口：**
- Web UI：\`/export/records\` 页面

---

## 流程关系图

\`\`\`
mp (公众号)
  │
  ├── subscription (订阅) ─── feed (RSS) ─── RSS 输出
  │
  └── article (文章)
        │
        ├── tag (标签) ─── tag cluster (标签聚类)
        │
        └── hot_topic (热点) ← 文章聚合

message_task (消息任务) → Webhook 通知
article → export / 图片转存
\`\`\`
`,
  "docs/overview": `# WeRSS 是什么

WeRSS 是一套面向**个人私有自托管**场景的微信公众号内容整理与分析系统：管理订阅来源，按配置获取文章信息，再提供摘要 RSS、标签、热点、消息通知与后台管理等能力。

## 合规与使用声明

本文档与仓库仅作**技术说明**，本项目仅提供自托管的软件工具和技术实现，不提供、销售或代运营任何第三方内容服务，也不代表项目维护者主动建立或发布特定公众号订阅关系。订阅源、采集方式、采集频率、访问凭据、数据保存范围、对外展示和再分发行为均由部署者或最终用户自行配置和管理；用户应仅在其有权访问、保存和使用相关内容的范围内使用本项目。

部署或使用本项目时，使用者应自行遵守微信公众平台、腾讯及其他相关平台的服务协议、开发者规则、robots/反爬策略、知识产权规则、个人信息保护要求以及所在地适用法律法规。不得将本项目用于绕过登录、验证码、访问控制或其他技术保护措施，不得用于批量抓取未授权内容、侵犯版权或将第三方内容用于未经许可的商业分发。

本项目中出现的“微信”“微信公众号”“腾讯”等名称仅用于描述兼容对象和技术场景，不表示本项目与相关权利方存在授权、合作、关联或背书关系。项目维护者不对具体部署场景的合法性、合规性或第三方权利风险作出保证；如使用者将系统用于公网服务、团队协作、商业用途或内容再传播，应在上线前自行完成法务、隐私和版权评估，并根据权利方请求及时删除、屏蔽或停止处理相关内容。

建议的低风险使用方式：
- 仅私有部署并启用登录认证，不提供匿名访问或真实内容演示
- 默认关闭正文采集、RSS 全文输出、图片转存和导出能力
- 不收费，不代他人部署为内容聚合站，不主动帮助他人分发第三方内容
- 收到平台或权利方通知后，及时删除相关内容并停止对应处理

## 核心能力

| 能力 | 说明 |
|------|------|
| 文章采集 | 支持按配置获取公众号文章信息，正文采集默认关闭 |
| RSS 输出 | 将文章摘要整理为标准 RSS 2.0 订阅源，全文输出需显式开启 |
| 标签提取 | 支持手动标签和基于 OpenAI 兼容接口的 AI 自动提取 |
| 标签聚类 | 基于 embedding 将标签按语义聚合 |
| 热点发现 | 从近期文章中发现科技热点主题 |
| 消息任务 | 支持钉钉、企业微信、飞书、Webhook 通知 |
| 导出存储 | 支持 PDF、Markdown 导出和 MinIO 图片转存，均建议按需开启 |

## 系统组成

\`\`\`
werss/
├── apis/          # HTTP API 路由层
├── core/          # 核心业务逻辑、数据模型、配置
├── jobs/          # 定时任务（采集、清理、消息推送）
├── driver/        # Playwright 浏览器驱动
└── web_ui/        # React 前端
    ├── src/views/ # 页面组件
    ├── src/api/   # 前端 API 调用封装
    └── src/router/# 路由配置
\`\`\`

## 核心数据流

\`\`\`
公众号订阅 → 定时采集 → 文章入库
                              ↓
              标签提取（手动/AI）← 公众号信息
                              ↓
              标签聚类 ← 标签管理 ← 标签筛选
                              ↓
              热点发现 ← 文章列表
                              ↓
              RSS输出 / 消息推送 / 导出
\`\`\`

## 已有的能力 vs 规划中的能力

**已有：**
- 文章采集（web/api/app）
- RSS 输出
- 标签系统（手动 + AI 提取）
- 标签聚类（基于 embedding）
- 热点发现
- 消息任务（Webhook 通知）
- 导出（PDF/Markdown）
- MinIO 图片转存

**规划中（未实现）：**
- 基于 GLiNER 的轻量化标签提取
- 微信内容自动生成
- 社区共建计划

---

## 谁应该看什么

| 角色 | 入口文档 |
|------|---------|
| 第一次接触项目 | 先看 \`docs/overview.md\`，再看 \`docs/concepts.md\` |
| 部署、运维 | \`docs/DEPLOYMENT.md\`、\`docs/QUICK_START.md\` |
| 后端、前端 | \`docs/dev/architecture-overview.md\`、\`docs/dev/backend-structure.md\` |
| 算法、大模型相关 | \`docs/concepts.md\`、\`docs/features/tag-system.md\`、\`docs/features/hot-topics.md\` |
| 主要用 Web 界面 | \`docs/ui/articles.md\`、\`docs/ui/subscriptions.md\` |

---

## 快速开始

**本地开发：**

\`\`\`bash
# 一键启动
./start_dev.sh

# 访问
# 前端: http://localhost:5174
# 后端: http://localhost:8001
# API文档: http://localhost:8001/api/docs
\`\`\`

**Docker 部署：**

\`\`\`bash
cp .env.example .env
docker compose up -d
# 访问: http://localhost:8001
\`\`\`

详细部署说明见 [docs/DEPLOYMENT.md](DEPLOYMENT.md)。

---

## 文档导航

### 第 1 层 - 导航层
- [README.md](../README.md)（仓库入口）
- [docs/DEPLOYMENT.md](./DEPLOYMENT.md)
- [docs/QUICK_START.md](./QUICK_START.md)

### 第 2 层 - 认知层
- [docs/overview.md](./overview.md)
- [docs/concepts.md](./concepts.md)
- [docs/workflows.md](./workflows.md)

### 第 3 层 - 功能层
- [docs/features/article-collection.md](./features/article-collection.md)
- [docs/features/tag-system.md](./features/tag-system.md)
- [docs/features/hot-topics.md](./features/hot-topics.md)
- [docs/features/rss-and-message-tasks.md](./features/rss-and-message-tasks.md)
- [docs/features/export-and-storage.md](./features/export-and-storage.md)

### 第 4 层 - UI 层
- [docs/ui/articles.md](./ui/articles.md)
- [docs/ui/subscriptions.md](./ui/subscriptions.md)
- [docs/ui/settings.md](./ui/settings.md)
- [docs/ui/tags.md](./ui/tags.md)
- [docs/ui/tag-clusters.md](./ui/tag-clusters.md)
- [docs/ui/hot-topics.md](./ui/hot-topics.md)
- [docs/ui/message-tasks.md](./ui/message-tasks.md)

### 第 5 层 - 开发层
- [docs/dev/architecture-overview.md](./dev/architecture-overview.md)
- [docs/dev/backend-structure.md](./dev/backend-structure.md)
- [docs/dev/frontend-structure.md](./dev/frontend-structure.md)
- [docs/dev/configuration-and-precedence.md](./dev/configuration-and-precedence.md)
- [docs/dev/jobs-and-schedulers.md](./dev/jobs-and-schedulers.md)
`,
  "docs/idea": `# WeRSS 的定位与想法

本文不讲实现细节，只说明 WeRSS 解决什么问题、在信息处理链条里站哪一环。

## 1. 问题从哪里来

微信公众号一直是中文互联网里高质量内容最密集的区域之一，尤其在：

- 科技
- 商业
- 产业观察
- 深度评论

这些方向上，很多真正有价值的内容并不会第一时间进入开放搜索系统。

现实问题主要有三个：

- 内容分散在封闭平台里，不方便持续跟踪
- 没有天然开放的订阅和结构化输出
- 高价值内容和大量普通更新混在一起，人工筛选成本很高

## 2. WeRSS 不是「再做一个订阅器」

若只把它当成「公众号转 RSS」，范围就窄了；更贴切的定位是：

- 把封闭内容源拉出来
- 变成可以继续分析和处理的结构化数据
- 再交给后续的标签、热点、通知、导出和知识整理能力使用

可以把它理解成一个内容感知层：

- 上游是微信公众号等封闭来源
- 中间是采集、清洗、结构化
- 下游是 RSS、热点发现、消息推送、知识整理

## 3. 当前已经做到什么

以现有代码为准，WeRSS 早就不止于「把文章拉下来」：

- 管理公众号来源、采集与入库
- 输出 RSS
- 标签提取与标签管理、标签聚类
- 热点发现、消息任务推送
- 部分对象存储与导出能力

整体更接近「内容侧底座」：可再接热点、通知、知识库等，而不是单点小工具。

## 4. 为什么“结构化”比“收集更多”更重要

如果只是无限采集文章，很快会变成另一个信息堆场。

真正有价值的是把文章继续变成更高层的信息对象，例如：

- 标签
- 聚类
- 热点主题
- 通知摘要
- 可导出的干净内容

这也是为什么项目里后面逐渐长出了：

- 标签系统
- 热点发现
- 消息任务

这些能力。

## 5. 这个项目适合服务谁

按当前形态，它最适合几类人：

- 想长期跟踪某一类公众号来源的人
- 想把内容做成 RSS / 通知 / 知识输入的人
- 需要从文章中提炼主题和热点的运营或研究者
- 想把公众号内容变成可供 AI 或知识系统进一步处理的数据源的人

## 6. 当前项目最合理的发展方向

在现有能力上继续加料，最顺的方向不是「堆更多爬虫」，而是把这条链走实：

\`来源 -> 文章 -> 标签 -> 聚类 / 热点 -> 输出 / 推送 / 进一步知识加工\`

也就是说，未来更值得加深的是：

- 主题发现能力
- 数据整理质量
- 推送和导出链路
- 面向知识系统的干净输入

而不必一味堆来源数量、拉取体量。

## 7. 一句话总结

WeRSS 的核心价值不是“把公众号文章搬出来”，而是把它们变成可以继续检索、聚合、分析和分发的内容资产。
`,
  "docs/concepts": `# 核心概念

下面把 WeRSS 里几个核心对象说透，读代码、看页面时，别把「公众号」「订阅」「Feed」「热点」「标签聚类」混为一谈。

## 1. 公众号 / mp

这是产品语义上的概念，表示一个微信公众号来源。

在实现里，它**没有**单独、稳定对外的「公众号」大表，职责多半落在 \`Feed\` 上。

所以读代码时，不妨先接受：

- 「公众号」
- 「订阅来源」
- 「Feed 记录」

说的是同一条主链、不同叫法。

## 2. Feed（订阅记录）

最核心的来源实体是 \`Feed\`。

位置：

- [core/models/feed.py](../core/models/feed.py)

它保存了：

- 公众号 ID
- 名称
- 封面图
- 简介
- 状态
- 同步时间
- 微信侧关联标识 \`faker_id\`

它既是：

- 采集入口
- 订阅管理页的数据源
- RSS 输出的上游实体

所以落地实现里，盯 \`Feed\` 比盯英文的 subscription 更靠谱。

## 3. subscription（订阅）

产品侧常说「订阅」，代码里却未必有一个叫 subscription 的稳态模型。页面上是：

- 订阅管理
- 添加订阅

真正落库、参与采集的，主要还是 \`Feed\`。

记一句就够：**界面说订阅，看代码先找 \`Feed\`。**

## 4. article（文章）

文章是整个系统的核心内容实体。

位置：

- [core/models/article.py](../core/models/article.py)
- [apis/article.py](../apis/article.py)
- [web_ui/src/views/ArticleListPage.tsx](../web_ui/src/views/ArticleListPage.tsx)

一篇文章通常包含：

- 标题
- 原文链接
- 摘要
- 正文
- 封面图
- 发布时间
- 所属公众号 ID

文章是这些能力的共同输入：

- 标签提取
- AI 过滤
- 热点发现
- RSS 输出
- 消息任务

## 5. tag（标签）

标签是文章的结构化关键词层。

位置：

- [core/models/tags.py](../core/models/tags.py)
- [apis/tags.py](../apis/tags.py)
- [web_ui/src/views/TagList.tsx](../web_ui/src/views/TagList.tsx)

一个标签可以：

- 来自 AI 提取
- 来自人工创建
- 关联多篇文章

标签是下游分析的基础信号，但它本身不是热点主题。

## 6. ArticleTag（文章-标签关联）

这是文章和标签之间的中间关系。

位置：

- [core/models/article_tags.py](../core/models/article_tags.py)

它表达的是：

- 一篇文章有多个标签
- 一个标签属于多篇文章

很多页面上的“标签文章数”或“文章标签列表”都依赖这张关系表统计。

## 7. AI 过滤状态

这是文章的另一层分析结果，不等于删除。

位置：

- [core/models/article_ai_filter.py](../core/models/article_ai_filter.py)

它的作用是：

- 判断一篇文章是保留、隐藏还是待定
- 让文章工作台可以默认隐藏低价值内容

所以“被过滤”不等于“被删掉”。

## 8. tag cluster（标签聚类）

标签聚类是“标签的聚合”，不是“文章的聚合”。

位置：

- [core/models/tag_clusters.py](../core/models/tag_clusters.py)
- [core/models/tag_cluster_members.py](../core/models/tag_cluster_members.py)
- [apis/tag_clusters.py](../apis/tag_clusters.py)

它表达的是：

- 多个语义相近的标签可以组成一个簇
- 一个簇有中心标签
- 聚类结果带版本号

标签聚类更适合解决：

- 标签碎片化
- 同义词 / 别名整理
- 语义归并

## 9. hot topic（热点主题）

热点主题是“文章的聚合”，不是标签的聚合。

位置：

- [core/models/hot_topics.py](../core/models/hot_topics.py)
- [core/models/hot_topic_runs.py](../core/models/hot_topic_runs.py)
- [core/models/hot_topic_articles.py](../core/models/hot_topic_articles.py)
- [apis/hot_topics.py](../apis/hot_topics.py)

它表达的是：

- 最近几天哪些文章其实在讲同一件事
- 每次分析都有运行记录
- 每个热点主题下面会挂一批文章

所以：

- \`tag cluster\` = 标签归堆
- \`hot topic\` = 文章归堆

这两个不要混。

## 10. message task（消息任务）

消息任务是系统的主动推送单元。

位置：

- [core/models/message_task.py](../core/models/message_task.py)
- [core/models/message_task_log.py](../core/models/message_task_log.py)
- [apis/message_task.py](../apis/message_task.py)

它至少包含这些要素：

- 任务名称
- 模板
- Webhook 地址
- cron 表达式
- 启用状态
- 关联公众号范围

消息任务的目标是：

- 按时间把文章整理后主动推送出去

## 11. RSS Feed

RSS 是文章的被动订阅输出层。

它不是独立内容源，而是基于 \`Feed + Article\` 动态生成的输出格式。

相关位置：

- [apis/rss.py](../apis/rss.py)

## 12. API Key

API Key 是程序化访问凭证。

位置：

- [core/models/api_key.py](../core/models/api_key.py)
- [apis/api_key.py](../apis/api_key.py)
- [web_ui/src/views/ApiKeyManagement.tsx](../web_ui/src/views/ApiKeyManagement.tsx)

它和登录态 JWT 不一样：

- JWT 是登录会话
- API Key 是长期给脚本或集成方使用的凭证

## 13. config override（配置覆盖）

这不是一个单独页面概念，但在系统里非常关键。

当前配置来源有多层：

- 配置文件
- \`.env\`
- 运行时环境变量
- 数据库中的配置覆盖

因此“设置页保存了配置”不等于“它就是系统唯一配置源”。

## 14. 概念关系图

\`\`\`text
Feed（订阅来源 / 公众号）
  -> Article（文章）
     -> Tag（标签）
        -> Tag Cluster（标签聚类）
     -> AI Filter（文章过滤状态）
     -> Hot Topic（热点主题）

Feed + Article
  -> RSS 输出

Article + Feed
  -> Message Task（主动推送）

User
  -> API Key
\`\`\`

## 15. 最重要的区分

如果只记住三件事，记住这三个区分就够了：

- \`Feed\` 是来源入口，不只是 RSS 概念
- \`tag cluster\` 是标签聚合，不是热点
- \`hot topic\` 是文章主题聚合，不是高频标签榜
`,
  "docs/UV_VENV_GUIDE": `# WeRSS 使用 uv 虚拟环境指南

## 📦 什么是 uv？

\`uv\` 是一个用 Rust 编写的极速 Python 包安装器和解析器，比传统的 \`pip\` 和 \`venv\` 快 10-100 倍。

## 🚀 快速开始

### 1. 安装 uv

\`\`\`bash
# 使用官方安装脚本（推荐）
curl -LsSf https://astral.sh/uv/install.sh | sh

# 或使用 pipx
pipx install uv

# 或使用 pip（不推荐）
pip install uv
\`\`\`

安装完成后，确保 \`uv\` 在 PATH 中：
\`\`\`bash
# 如果使用官方安装脚本，uv 会被安装到 ~/.cargo/bin
export PATH="$HOME/.cargo/bin:$PATH"

# 验证安装
uv --version
\`\`\`

### 2. 创建虚拟环境

\`\`\`bash
cd /path/to/werss

# 使用 uv 创建虚拟环境（默认创建 .venv）
uv venv

# 或指定名称
uv venv venv

# 或指定 Python 版本
uv venv --python 3.11
\`\`\`

### 3. 激活虚拟环境

\`\`\`bash
# Linux/macOS
source .venv/bin/activate

# Windows
.venv\\Scripts\\activate
\`\`\`

### 4. 安装依赖

**方式一：使用 uv pip（推荐）**
\`\`\`bash
# 激活虚拟环境后
uv pip install -r requirements.txt

# 使用国内镜像加速
uv pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
\`\`\`

**方式二：使用 uv sync（如果使用 pyproject.toml）**
\`\`\`bash
# uv sync 会根据 pyproject.toml 自动创建虚拟环境并安装依赖
uv sync
\`\`\`

### 5. 启动服务

\`\`\`bash
# 确保虚拟环境已激活
source .venv/bin/activate

# 设置环境变量
export DB=postgresql://user:pass@localhost:5432/werss_db
export WERSS_USERNAME=admin
export WERSS_PASSWORD=admin@123

# 初始化数据库（首次运行）
python main.py -init True

# 启动服务
python main.py -job True -init False
\`\`\`

## 📝 完整示例

\`\`\`bash
# 1. 进入项目目录
cd /path/to/werss

# 2. 创建虚拟环境
uv venv

# 3. 激活虚拟环境
source .venv/bin/activate

# 4. 安装依赖
uv pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 5. 配置环境变量
export DB=postgresql://\${POSTGRES_USER}:\${POSTGRES_PASSWORD}@localhost:5432/werss_db
export WERSS_USERNAME=admin
export WERSS_PASSWORD=admin@123
export DEBUG=True
export AUTO_RELOAD=True

# 6. 初始化数据库（首次运行）
python main.py -init True

# 7. 启动服务
python main.py -job True -init False
\`\`\`

## 🔄 日常使用

### 激活虚拟环境

每次打开新的终端时，需要重新激活虚拟环境：

\`\`\`bash
cd /path/to/werss
source .venv/bin/activate
\`\`\`

### 更新依赖

\`\`\`bash
# 激活虚拟环境后
uv pip install -r requirements.txt --upgrade
\`\`\`

### 添加新依赖

\`\`\`bash
# 方式一：手动编辑 requirements.txt，然后安装
uv pip install -r requirements.txt

# 方式二：使用 uv add（如果使用 pyproject.toml）
uv add package_name
\`\`\`

### 退出虚拟环境

\`\`\`bash
deactivate
\`\`\`

## 🆚 uv vs 传统方式对比

| 操作 | 传统方式 | uv 方式 |
|------|---------|---------|
| 创建虚拟环境 | \`python3 -m venv venv\` | \`uv venv\` |
| 安装依赖 | \`pip install -r requirements.txt\` | \`uv pip install -r requirements.txt\` |
| 速度 | 较慢 | 快 10-100 倍 |
| 激活方式 | \`source venv/bin/activate\` | \`source .venv/bin/activate\` |

## 💡 优势

1. **速度更快**：依赖安装和解析速度提升 10-100 倍
2. **自动管理**：自动处理 Python 版本和依赖冲突
3. **兼容性好**：完全兼容 pip 和 requirements.txt
4. **缓存机制**：自动缓存下载的包，加快后续安装

## ⚠️ 注意事项

1. **虚拟环境位置**：uv 默认创建 \`.venv\` 目录（而不是 \`venv\`）
2. **激活方式相同**：激活方式与传统 venv 完全相同
3. **依赖文件**：仍然使用 \`requirements.txt\`，无需修改
4. **Python 版本**：确保系统已安装 Python 3.11+

## 🔧 故障排除

### 问题1: uv 命令未找到

\`\`\`bash
# 检查是否在 PATH 中
which uv

# 如果未找到，添加到 PATH
export PATH="$HOME/.cargo/bin:$PATH"

# 或重新安装
curl -LsSf https://astral.sh/uv/install.sh | sh
\`\`\`

### 问题2: 虚拟环境激活失败

\`\`\`bash
# 检查虚拟环境是否存在
ls -la .venv/

# 重新创建虚拟环境
rm -rf .venv
uv venv
source .venv/bin/activate
\`\`\`

### 问题3: 依赖安装失败

\`\`\`bash
# 使用国内镜像
uv pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或清除缓存后重试
uv cache clean
uv pip install -r requirements.txt
\`\`\`

## 📚 相关文档

- [uv 官方文档](https://docs.astral.sh/uv/)
- [WeRSS 快速开始指南](QUICK_START.md)
`,
  "docs/TAG_CLUSTER_CHECKLIST": `# 标签聚类上线检查清单

本文档用于在启用或重新部署标签聚类能力前做快速核对，避免“页面能打开，但聚类数据没准备好”的情况。

## 1. 上线前检查

- 确认标签聚类相关表已经创建完成：
  - \`tag_profiles\`
  - \`tag_embeddings\`
  - \`tag_similarities\`
  - \`tag_clusters\`
  - \`tag_cluster_members\`
- 确认已经配置至少一种 embedding 提供方：
  - \`TAG_CLUSTER_EMBEDDING_PROVIDER=bigmodel\` 并配置 \`BIGMODEL_API_KEY\`
  - 或 \`TAG_CLUSTER_EMBEDDING_PROVIDER=doubao\` 并配置 \`DOUBAO_API_KEY\`
- 确认聚类功能开关已启用：
  - \`TAG_CLUSTER_ENABLED=true\`

## 2. 本地验证

- 先做 Python 语法检查：

\`\`\`bash
python3 -m py_compile \\
  apis/tag_clusters.py \\
  core/tag_cluster.py \\
  core/embedding/*.py \\
  core/models/tag_*.py \\
  scripts/build_tag_clusters.py
\`\`\`

- 再做前端构建检查：

\`\`\`bash
cd web_ui
npm run build
\`\`\`

- 最后执行一次离线重建：

\`\`\`bash
python3 scripts/build_tag_clusters.py
\`\`\`

## 3. 部署后验证

- 打开 \`/tag-clusters\` 页面，确认聚类列表能正常加载
- 进入任意一个聚类详情页
- 确认以下区域都有数据：
  - 聚类摘要
  - 成员标签
  - 相似标签
  - 合并建议
- 点击“导出 JSON”，确认导出的内容包含：
  - 聚类元数据
  - 成员列表
  - 合并建议

## 4. 常见异常

### 页面能打开，但列表为空

优先检查：

- 聚类重建是否真的执行过
- embedding 提供方是否配置正确
- 标签表中是否已有足够标签数据

### 重建报错提示未配置 API Key

说明 embedding 提供方已经启用，但对应密钥未配置完整。先补 \`.env\` 或运行环境变量，再重建。

### 聚类详情能开，但没有相似标签或合并建议

优先检查：

- 标签相似度表是否生成
- 当前标签数据是否过少
- 阈值是否过高
`,
  "docs/ROADMAP": `# 发展路线与待办事项

**维护说明：** 本项目的日常运维（包括 Issue/PR 处理、文档维护等）主要由 **Claude Code**、**Codex** 与 **GLM** 等工具链辅助实现；路线图中的研究课题与共建项目欢迎社区与课题组同步推进。

\`README\` 中提供了本路线图的**摘要表格**；完整的待办任务与进度以本文档为准。已完成项请标记为 \`[x]\` 或移动到“已完成”小节。

---

本文跟踪教学、研究与社区协作相关的待办；与 \`README\` 中的摘要表对照阅读即可。

## 1. 微信内容自动生成（支持特定主题）

**背景：** 本功能旨在根据指定的 **主题（Topic）** 自动生成适合微信公众号发布的文章草稿或结构化大纲。

| 状态 | 任务描述 |
| :--- | :--- |
| [ ] | 明确输入输出参数：包括主题、受众群体、字数要求、摘要/标签生成等，并对接现有文章入库流程。 |
| [ ] | 对齐采集与编辑流程：确保生成的内容能复用 \`Article\` 模型、共享数据库并支持审核状态流转。 |
| [ ] | 安全与合规保障：建立生成内容的免责声明、人工审核节点以及敏感词/事实核查策略。 |
| [ ] | 接口封装：将该功能封装为标准的 API 或脚本工具，并完善使用说明文档。 |
| [ ] | 样例准备：提供 1～2 个典型主题及预期输出示例，便于功能验收。 |

**建议标签（Issue）：** \`skill\`, \`content-generation\`, \`wechat\`

---

## 2. 智能标签系统：基于 GLiNER 的轻量化方案

**背景：** 探索比现有方案更轻量的文本处理方案。基于 **GLiNER** 类模型提取关键词与实体，并与当前的标签提取方案（如 KeyBERT、LLM）进行结果比对或级联增强。

| 状态 | 任务描述 |
| :--- | :--- |
| [ ] | 调研 GLiNER 在中文及多语种场景下的适用范围、推理成本与依赖库体积。 |
| [ ] | 建立评测标准：与现有标签提取行为对齐，制定离线评测指标（如 F1 值）和人工核对协议。 |
| [ ] | 实现后端集成：支持通过配置切换至 GLiNER 提取方法，并具备失败回退机制。 |
| [ ] | 资源文档化：说明模型来源、许可协议，并支持在 Docker 镜像中按需加载。 |
| [ ] | （可选）整理技术方案或报告素材，方便学术合作者复现研究结果。 |

**建议标签（Issue）：** \`research\`, \`tags\`, \`gliner\`, \`performance\`

---

## 3. 社区共建：面向开发者与研究者

**目标：** 寻求愿意共同维护代码、实验与文档的长期贡献者（包括但不限于课题组、工程师、博士生）。

| 状态 | 任务描述 |
| :--- | :--- |
| [x] | 在 README 显著位置增加协作邀请说明。 |
| [ ] | 明确贡献领域：如 NLP 数据处理、微信生态工具、容器化运维、前端开发、模型评测等。 |
| [ ] | 建立沟通机制：完善 Issue 模板、讨论区（Discussions）或专门的联系渠道。 |
| [ ] | 维护入门任务：标注 \`good first issue\` 或 \`help wanted\` 标签，降低参与门槛。 |
| [ ] | （可选）与相关实验室或开源组织建立合作分工机制。 |

**建议标签（Issue）：** \`community\`, \`maintainer-wanted\`, \`collaboration\`

---

## 已完成（迁移时填写）

<!-- 例如：
- [x] 示例条目
-->

---

若你希望把某一条拆成 GitHub Issue，可直接复制表格中的待办项作为 Issue 标题，并打上对应建议标签。
`,
  "docs/QUICK_START": `# WeRSS 开发环境快速开始

本文目标只有一个：尽快把本仓库在本地跑起来。不穷举所有部署形态，只写当前代码里**最稳、最常用**的开发路径。

## 1. 你会启动什么

本地开发通常分成两部分：

- 后端：FastAPI，默认端口 \`8001\`
- 前端：React + Vite，默认端口 \`5174\`

最常见的访问地址：

- 前端开发页：\`http://localhost:5174\`
- 后端接口：\`http://localhost:8001\`
- API 文档：\`http://localhost:8001/api/docs\`
- 健康检查：\`http://localhost:8001/api/health\`

## 2. 前置条件

至少准备这些：

- Python 3.11+
- Node.js 20+
- 一个可用的数据库
  开发阶段 SQLite、PostgreSQL 都能用；若要尽量贴近生产、把整条链路跑通，建议用 PostgreSQL。

如果你要跑微信采集和浏览器抓取，还需要本机能正常运行 Playwright 依赖。

## 3. 最快启动方式

如果你只想先把前后端都拉起来，直接用仓库自带脚本：

\`\`\`bash
./start_dev.sh
\`\`\`

这个脚本会尝试：

- 检查环境
- 启动后端
- 启动前端
- 打开开发地址

适合第一次快速看页面。

## 4. 推荐的手动开发方式

如果你是开发者，建议手动分开启动，这样排查更清楚。

### 4.1 Python 环境

推荐用 \`uv\`：

\`\`\`bash
uv venv
source .venv/bin/activate
uv sync
\`\`\`

如果你不用 \`uv\`，也可以自己用 \`venv + pip\`，但当前仓库已经有 \`pyproject.toml\` 和 \`uv.lock\`，优先按这一套来。

### 4.2 初始化数据库

首次运行建议先做初始化：

\`\`\`bash
python main.py -init True
\`\`\`

这一步会确保数据库表存在，并执行初始化逻辑。

### 4.3 启动后端

\`\`\`bash
python main.py -job True -init False
\`\`\`

说明：

- \`-job True\`
  表示同时启用后台任务和调度器
- \`-init False\`
  表示这次启动不再重复初始化

如果你只想调 API，不想跑后台任务，也可以把 \`-job\` 关掉：

\`\`\`bash
python main.py -job False -init False
\`\`\`

### 4.4 启动前端

\`\`\`bash
cd web_ui
pnpm install
pnpm dev
\`\`\`

前端默认监听：

- \`http://localhost:5174\`

## 5. 构建生产前端资源

如果你改了前端，并且想把构建产物同步进后端 \`static/\`，执行：

\`\`\`bash
cd web_ui
pnpm build:deploy
\`\`\`

这一步不是开发调试必需，但如果你要验证“后端直接托管前端资源”的形态，就需要它。

## 6. 配置从哪里来

开发时最常见的配置来源有这些：

- \`.env\`
- \`config.yaml\`
- 环境变量
- 数据库中的配置覆盖

建议起步时先看：

- \`.env.example\`
- \`config.example.yaml\`
- [配置与优先级](./dev/configuration-and-precedence.md)

最容易踩的坑是：

- 你以为改了 \`config.yaml\`
- 实际被环境变量或数据库覆盖了

## 7. 典型开发链路

### 只看页面

\`\`\`bash
./start_dev.sh
\`\`\`

### 改后端接口

\`\`\`bash
source .venv/bin/activate
python main.py -job False -init False
\`\`\`

然后用：

- \`http://localhost:8001/api/docs\`

直接验证接口。

### 改前端页面

\`\`\`bash
cd web_ui
pnpm dev
\`\`\`

配合后端一起跑即可。

### 改定时任务或采集链路

\`\`\`bash
python main.py -job True -init False
\`\`\`

因为不开 \`job\` 时，你根本看不到那部分真实行为。

## 8. Docker 开发什么时候用

仓库提供了：

- \`docker-compose.yml\`
- \`docker-compose.dev.yml\`
- \`docker-compose.app-only.yml\`

Docker 更适合：

- 统一环境
- 接近部署环境
- 带 Postgres / MinIO 一起联调

但如果你只是改页面或接口，优先本地直跑，反馈更快。

完整部署细节请看：

- [DEPLOYMENT.md](./DEPLOYMENT.md)

## 9. 常见问题

### 后端能起，前端打不开

优先检查：

- 是否在 \`web_ui/\` 下执行了 \`pnpm install\`
- \`5174\` 端口是否被占用

### 页面打开了，但接口都报错

优先检查：

- 后端是否已经启动
- \`8001\` 端口是否可访问
- 登录态是否存在

### 采集相关功能不工作

优先检查：

- 是否用 \`-job True\` 启动
- 微信登录状态是否有效
- Playwright 依赖是否正常

### 改了前端但后端托管页面没变化

因为开发模式和后端静态资源不是同一套产物。要让后端托管页面更新，需要执行：

\`\`\`bash
cd web_ui
pnpm build:deploy
\`\`\`

## 10. 建议阅读顺序

启动成功后，建议继续看：

- [overview.md](./overview.md)
- [workflows.md](./workflows.md)
- [backend-structure.md](./dev/backend-structure.md)
- [frontend-structure.md](./dev/frontend-structure.md)
`,
  "docs/MCP_REMOTE": `# WeRSS 远程 MCP 接入

WeRSS 现在暴露了一个远程 MCP 入口：

- \`GET /mcp\`
- \`POST /mcp\`

这是一个基于 Streamable HTTP 的 MCP endpoint。当前实现优先支持工具调用；如果客户端只发 \`POST\`，也可以正常工作。

## 环境变量

建议配置以下变量：

- \`WERSS_MCP_TOKEN\`
  - MCP 访问令牌
  - 如果设置了，所有请求都必须带 \`Authorization: Bearer <token>\` 或 \`X-MCP-Token: <token>\`
- \`MCP_ALLOWED_ORIGINS\`
  - 允许的 Origin 列表，逗号分隔
  - 例如：\`https://your-domain.com,https://app.your-domain.com\`

## 客户端配置

客户端可直接指向：

\`\`\`text
https://your-domain.com/mcp
\`\`\`

请求头：

\`\`\`text
Authorization: Bearer YOUR_MCP_TOKEN
\`\`\`

## 已暴露工具

- \`articles.list\`
- \`articles.get\`
- \`articles.toggle_status\`
- \`articles.ai_filter\`
- \`articles.ai_filter_restore\`
- \`tags.list\`
- \`tags.get\`
- \`tags.toggle_status\`
- \`tag_clusters.list\`
- \`tag_clusters.get\`
- \`tag_clusters.visualization\`
- \`tag_clusters.network\`

## 说明

- \`articles.ai_filter\` 会执行 AI 过滤并写入数据库
- \`articles.ai_filter_restore\` 会恢复 AI 过滤结果并把文章重新启用
- \`tags.toggle_status\` 会切换标签启用/禁用状态
- \`tag_clusters.visualization\` 和 \`tag_clusters.network\` 直接返回可视化数据

`,
  "docs/DEPLOYMENT": `# WeRSS 部署指南

WeRSS 支持两种部署模式，按需选择：

| 模式 | 文件 | 适用场景 |
|------|------|----------|
| **完整栈** | \`docker-compose.yml\` | 新用户一键启动：Traefik + Postgres + MinIO + WeRSS（默认经 Traefik 暴露 HTTPS） |
| **仅应用** | \`docker-compose.app-only.yml\` | 已有外部数据库/对象存储/反代 |
| 开发覆盖 | \`docker-compose.dev.yml\`（叠加在完整栈上） | 本地开发，启用 DEBUG，数据目录隔离，并为 \`werss\` 映射 \`8001\` 便于直连 |

---

## 模式一：完整栈

包含 **Traefik**（80/443）、PostgreSQL、MinIO 和 WeRSS。\`werss\` 不默认映射 \`8001\`，由 Traefik 按 \`.env\` 中的 \`WERSS_HOST\` 做 Host 匹配并转发到容器内 \`8001\`；证书由 Traefik 的 Let’s Encrypt（TLS-ALPN）申请。

\`\`\`bash
cp .env.example .env   # 设置 WERSS_HOST、ACME_EMAIL 等；见下文 Traefik 相关变量
docker compose up -d
\`\`\`

- **生产/公网域名**：将 \`WERSS_HOST\` 设为该域名，DNS A 记录指向服务器，开放 80、443。
- **仅本机试用**：可叠加 \`docker-compose.dev.yml\`，通过映射的 \`http://localhost:8001\` 访问应用（不经 Traefik）；若仍走 Traefik，Let’s Encrypt 对 \`localhost\` 无法签发，需使用真实域名或自行调整路由。

数据目录默认在 \`./data/\`（含 \`traefik/acme\` 证书存储），可通过环境变量覆盖。

### 本地开发

在完整栈基础上叠加开发配置（DEBUG=True、容器名加 \`-dev\` 后缀、数据目录隔离）：

\`\`\`bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
\`\`\`

---

## 模式二：仅应用

只启动 WeRSS 容器，数据库和对象存储由外部提供。

\`\`\`bash
# 编辑 .env，设置 DB 和 MinIO 相关变量
docker compose -f docker-compose.app-only.yml up -d --build
\`\`\`

### 数据库连接

WeRSS 唯一读取的连库变量是 \`DB\`。在 \`.env\` 中设置：

\`\`\`env
DB=postgresql://user:password@host:5432/werss_db
\`\`\`

#### 容器内如何访问宿主机上的数据库

\`docker-compose.app-only.yml\` 已配置 \`extra_hosts: host.docker.internal:host-gateway\`。

| 宿主机 Postgres 端口映射 | 容器内连法 |
|--------------------------|-----------|
| \`0.0.0.0:5432->5432\` 或 \`5432:5432\` | \`DB=...@host.docker.internal:5432/...\` |
| \`127.0.0.1:5432->5432\`（仅回环） | \`host.docker.internal\` 不通，需改为 Docker 网络方式（见下文） |

**注意：** 容器内 \`localhost\` 是容器自己，不是宿主机。

#### 通过 Docker 网络连接

如果 Postgres 跑在另一个 Docker Compose 栈里，且端口只绑到 \`127.0.0.1\`，可以让 WeRSS 加入该网络：

\`\`\`bash
# 查看 Postgres 容器所在的网络名
docker inspect postgres --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}} {{end}}'

# 设置 WERSS_EXTERNAL_NETWORK 并启动
WERSS_EXTERNAL_NETWORK=your_postgres_network docker compose -f docker-compose.app-only.yml up -d
\`\`\`

此时 \`.env\` 中的 \`DB\` 用服务名访问：

\`\`\`env
DB=postgresql://user:password@postgres:5432/werss_db
\`\`\`

### Traefik 反向代理

\`docker-compose.app-only.yml\` 内置了 Traefik labels，默认不启用。要接入已有 Traefik：

1. 查清 Traefik 所在的 Docker 网络名和 entrypoints/certresolver 名称：

\`\`\`bash
docker inspect <traefik容器> --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}} {{end}}'
\`\`\`

2. 在 \`.env\` 中配置：

\`\`\`env
WERSS_EXTERNAL_NETWORK=traefik           # Traefik 所在网络
WERSS_TRAEFIK_ENABLE=true
WERSS_HOST=werss.example.com
WERSS_TLS_RESOLVER=letsencrypt           # 与 Traefik 静态配置一致
WERSS_ENTRYPOINTS=websecure              # 默认 websecure
\`\`\`

3. 启动后验证：

\`\`\`bash
# Traefik 容器内应能访问 werss
docker exec <traefik容器> wget -qO- http://werss:8001/api/health

# 浏览器访问 https://werss.example.com（DNS A 记录须指向服务器公网 IP）
\`\`\`

由 Traefik 提供 HTTPS 时，可注释掉 \`.env\` 中的 \`WERSS_PUBLISH_PORT\` 或将其设为不暴露的端口，避免多余暴露。

### MinIO / 对象存储

在 \`.env\` 中配置与容器内可访问的 endpoint：

\`\`\`env
MINIO_ENDPOINT=your-minio-host:9000
MINIO_ACCESS_KEY=your_key
MINIO_SECRET_KEY=your_secret
MINIO_BUCKET=werss-images
MINIO_PUBLIC_URL=https://cdn.example.com
\`\`\`

不要沿用 \`minio:9000\`，除非 WeRSS 和 MinIO 在同一 Docker 网络中。

---

## 配置优先级

运行时 \`cfg.get()\` 的取值顺序：

| \`WERSS_ENV_OVERRIDES_DB\` | 行为 |
|--------------------------|------|
| 未设置 / \`false\`（默认） | 若 PostgreSQL 表 \`config_management\` 中有该键，优先用数据库；否则用 \`config.yaml\` + 环境变量。连接串 \`db\` 从不走表覆盖。 |
| \`true\` | 先用 yaml + 环境变量；仅当值为空时才回退到 \`config_management\` 表。 |

控制台保存的配置写在 \`config_management\` 表里。若你改了 \`.env\` 仍看到旧值，检查表中是否仍有该键。

---

## 可配置环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| \`WERSS_PUBLISH_PORT\` | \`8001\` | 对外映射的端口 |
| \`WERSS_CONTAINER_NAME\` | \`werss\` | 容器名 |
| \`WERSS_DATA_DIR\` | \`./data/werss-data\` | 数据目录 |
| \`WERSS_LOGS_DIR\` | \`./logs\` | 日志目录 |
| \`WERSS_EXTERNAL_NETWORK\` | — | 加入已有 Docker 网络（Postgres、Traefik 等） |
| \`WERSS_TRAEFIK_ENABLE\` | \`false\` | 启用 Traefik labels |
| \`WERSS_HOST\` | — | Traefik Host 规则域名 |
| \`WERSS_TLS_RESOLVER\` | \`letsencrypt\` | Traefik 证书解析器名 |
| \`WERSS_ENTRYPOINTS\` | \`websecure\` | Traefik entrypoints |

### 完整栈（\`docker-compose.yml\`）与 Traefik

| 变量 | 默认值 | 说明 |
|------|--------|------|
| \`WERSS_HOST\` | \`localhost\` | Traefik \`Host()\` 规则；公网部署请改为你的域名 |
| \`ACME_EMAIL\` | \`admin@example.com\` | Let’s Encrypt 注册邮箱 |
| \`TRAEFIK_HTTP_PORT\` | \`80\` | 宿主机 HTTP 端口 |
| \`TRAEFIK_HTTPS_PORT\` | \`443\` | 宿主机 HTTPS 端口 |
| \`TRAEFIK_CONTAINER_NAME\` | \`werss-traefik\` | Traefik 容器名 |
| \`TRAEFIK_LOG_LEVEL\` | \`WARN\` | Traefik 日志级别 |

首次使用前请创建 \`data/traefik/acme\` 与 \`data/traefik/dynamic\`（或挂载后由 Docker 创建），并确保 \`acme.json\` 可写（\`chmod 600\` 常见）。

---

## 文章存储周期清理

按入库时间（\`created_at\`，默认）或发布时间（\`publish_time\`）删除超过保留天数的文章，并清理 \`article_tags\`、\`article_ai_filters\` 关联行。默认不启用，避免升级后误删历史数据。

**生效条件（同时满足）：**

- \`config.yaml\` 中 \`article.retention.enabled\` 为 \`true\`，或环境变量 \`ARTICLE_RETENTION_ENABLED=true\`
- \`server.enable_job\` 为 \`true\`（如 \`ENABLE_JOB=True\`）
- 进程启动时带 \`-job True\`（与现有定时采集一致）

**常用环境变量：**

| 变量 | 默认值 | 说明 |
|------|--------|------|
| \`ARTICLE_RETENTION_ENABLED\` | \`false\` | 是否启用定时清理 |
| \`ARTICLE_RETENTION_DAYS\` | \`7\` | 保留最近多少天 |
| \`ARTICLE_RETENTION_BASIS\` | \`created_at\` | \`created_at\` 或 \`publish_time\` |
| \`ARTICLE_RETENTION_CRON\` | \`0 4 * * *\` | 五段 cron（分 时 日 月 周） |
| \`ARTICLE_RETENTION_BATCH_SIZE\` | \`300\` | 每批处理条数 |

删除方式与 \`article.true_delete\` 一致：\`true\` 为物理删除，\`false\` 仅将 \`status\` 标为已删除。

---

## MinIO 与公众号图片转存

- **基础设施**：\`minio.enabled\`、\`MINIO_ENDPOINT\`、\`MINIO_ACCESS_KEY\` 等在 \`config.yaml\` / 环境变量中配置；未启用或连不上时不会初始化 MinIO 客户端。
- **业务开关**：\`minio.store_article_images\`（环境变量 \`MINIO_STORE_ARTICLE_IMAGES\`，默认 \`false\`）。为 \`true\` 且 MinIO 可用时，将微信侧正文/封面/公众号头像等转存到对象存储；为 \`false\` 时保留微信图片 URL，不占用存储。建议仅在确认有权保存相关图片或完全私有归档时开启。
- **系统设置**：可在 Web **应用设置** 中切换同一配置项（写入 \`config_management\`）。若 \`WERSS_ENV_OVERRIDES_DB=true\`，以环境变量 / yaml 为准，控制台仅在对应键未设置时生效（见上文「配置优先级」）。

---

## 与外部数据库共库注意事项

如果 WeRSS 与其他项目共用同一个 PostgreSQL 数据库，且该数据库中 \`articles.id\` 是 \`integer\`（如 Prisma 生成的），而 WeRSS 使用字符串主键（\`公众号id-文章id\`），\`article_tags\` 表不会在数据库层声明外键约束，关联由应用层 \`JOIN\` 维护。更稳妥的做法是为 WeRSS 使用独立数据库。

---

## 常见问题

| 现象 | 排查 |
|------|------|
| \`Connection refused\` 连不上数据库 | 检查 Postgres 端口映射是否为 \`0.0.0.0:5432\`；若为 \`127.0.0.1:5432\` 需通过 Docker 网络连接 |
| Traefik 502 / 无路由 | \`werss\` 是否在 Traefik 网络中；\`loadbalancer.server.port\` 是否为 \`8001\`；容器是否健康 |
| 证书不下发 | \`certresolver\` 名是否正确；80 端口是否通（HTTP-01）；防火墙 |
| 只要 HTTP 内网访问 | \`WERSS_ENTRYPOINTS=web\`，不设 \`WERSS_TLS_RESOLVER\` |
| 健康检查 404 | 确认镜像包含 \`/api/health\` 端点（需 rebuild） |
`,
  "docs/CONTRIBUTING": `# 贡献指南

本文档面向准备给 \`werss\` 提交文档、前端、后端、采集链路或任务调度改动的开发者。目标很简单：

- 先理解你要改的那一层
- 用最小改动解决问题
- 在提交前做对口验证
- 不把旧文档、旧路径、旧认知继续带回仓库

## 1. 开始之前先做什么

第一次接触仓库，建议先读这一组文档：

1. [README.md](../README.md)
2. [overview.md](./overview.md)
3. [concepts.md](./concepts.md)
4. [QUICK_START.md](./QUICK_START.md)

如果你改的是特定模块，再补读对应文档：

- 后端接口或数据流：
  [backend-structure.md](./dev/backend-structure.md)、
  [data-model-overview.md](./dev/data-model-overview.md)
- 前端页面或交互：
  [frontend-structure.md](./dev/frontend-structure.md)
- 调度器、采集任务、保留策略：
  [jobs-and-schedulers.md](./dev/jobs-and-schedulers.md)
- 配置项、环境变量、部署差异：
  [configuration-and-precedence.md](./dev/configuration-and-precedence.md)、
  [DEPLOYMENT.md](./DEPLOYMENT.md)
- 标签、热点、消息任务这类业务能力：
  [tag-system.md](./features/tag-system.md)、
  [hot-topics.md](./features/hot-topics.md)、
  [rss-and-message-tasks.md](./features/rss-and-message-tasks.md)

## 2. 先确认你改的是什么

提交前先把改动归类。不要一边改页面，一边顺手改接口语义，再顺手改数据库字段。

常见改动类型：

- 文档补全或纠错
- 前端页面与交互
- 后端 API
- 采集逻辑
- 定时任务
- 数据模型
- 部署与配置

不同类型的改动，验证方式不一样。\`服务能启动\` 不等于 \`功能改对了\`。

## 3. 分支与提交建议

建议一个功能或一个修复用一个独立分支：

\`\`\`bash
git checkout -b feature/your-change
\`\`\`

不要把这些无关事情混在同一轮里：

- 文档重写
- 接口行为变更
- UI 调整
- 配置语义变更
- 大规模重构

如果你确实必须跨层修改，至少要在 PR 里把边界讲清楚。

## 4. 改动前的基本原则

- 小改动优先，不要一口气做大重构
- 优先复用已有模块和现有模式
- 不要把复杂业务塞进路由层
- 不要把页面逻辑散落到多个临时组件里
- 配置改动必须同步更新示例和文档
- 当前代码是第一事实来源，不要按旧 README 或旧印象改

这个仓库历史演进比较多，最常见的问题不是“不会写”，而是：

- 以为系统还是旧结构
- 以为某个接口或页面还是旧行为
- 文档已经更新，但代码没有对应修改
- 代码改了，文档还在讲上一版

## 5. 提交前要做什么验证

下面这张表是最实用的最小验证清单。

| 改动类型 | 最少要验证什么 |
| --- | --- |
| 文档 | 路径没写错、没有继续引用不存在的页面/接口、内容与当前代码一致 |
| 前端 | 页面能打开、路由能进入、相关接口正常、\`pnpm build\` 能过 |
| 后端 | 服务能启动、\`/api/health\` 正常、你改的接口能通、异常返回合理 |
| 采集逻辑 | 采集链路能走通、失败时有日志或显式错误、不会静默吞掉问题 |
| 定时任务 | \`-job True\` 下能正常注册和执行、调度器状态正常 |
| 数据模型 | 初始化或迁移流程能走通，相关查询和写入不报错 |
| 部署与配置 | 环境变量优先级、默认值、Docker/本地差异都已核对 |

### 文档改动

至少确认：

- 引用的文档和代码路径存在
- 文字描述和当前实现一致
- 没有残留旧页面名、旧 API 路径、旧模型名
- 如果改了 \`docs-site\`，运行文档校验

推荐执行：

\`\`\`bash
cd docs-site
npm run check:docs
\`\`\`

### 前端改动

至少确认：

- 对应页面能正常打开
- 路由可进入
- 接口调用没有明显回归
- 构建通过

推荐执行：

\`\`\`bash
cd web_ui
pnpm build
\`\`\`

### 后端改动

至少确认：

- 服务能启动
- \`/api/health\` 返回正常
- 你改的接口能通
- 异常输入不会直接 500

常用启动方式：

\`\`\`bash
python main.py -init True
python main.py -job False -init False
\`\`\`

### 采集和任务调度改动

至少确认：

- \`-job True\` 下任务会注册
- 调度器状态正常
- 登录态失效、接口异常时不会静默失败
- 关键日志可用于排查

建议补查：

- [jobs-and-schedulers.md](./dev/jobs-and-schedulers.md)
- [workflows.md](./workflows.md)

## 6. 文档和代码要一起维护

如果你改了以下任何内容，通常都应该同步更新文档：

- 页面入口
- 路由
- API 路径
- 配置项
- 数据模型语义
- 定时任务行为
- 采集链路

最常见的问题不是“没写新文档”，而是：

- 旧文档没删
- 页面已经变了，文档还在讲上一版
- 新功能做了，但 README 和文档导航完全看不出来

## 7. 提交说明怎么写

不要写这种提交信息：

\`\`\`bash
git commit -m "fix"
git commit -m "update"
\`\`\`

至少要说明为什么改：

\`\`\`bash
git commit -m "Clarify hot topic docs and align links with current docs site routing"
\`\`\`

如果你在本仓库直接提交代码，建议遵守仓库里的 Lore 提交规范。重点不是形式感，而是把这些讲清楚：

- 为什么要改
- 有什么外部约束
- 放弃过什么方案
- 验证了什么
- 还没验证什么

## 8. Pull Request 里建议写什么

一个可读的 PR，至少应包含：

- 改动目的
- 改了哪些模块
- 如何验证
- 已知风险或未覆盖点

如果改的是界面，最好附截图或录屏。  
如果改的是接口，最好附请求示例或返回结构说明。  
如果改的是文档站，最好说明：

- 改了哪些入口
- 有无新增路由
- \`npm run check:docs\` 是否通过

## 9. 什么情况下应该先开 Issue

这些情况建议先开 Issue 或先对齐方案，再开始动手：

- 新增较大功能
- 改动现有接口语义
- 改动数据库字段语义
- 引入新依赖
- 要动采集主链路
- 要改部署方式或默认配置行为

如果只是小修复、文档补全、错链修正、明显 bug，一般可以直接做。

## 10. 对外协作时最重要的一点

请始终以当前代码为准。

不要按这些东西脑补仓库结构：

- 旧 README
- 旧截图
- 外部文章
- 以前的使用记忆

这个项目已经不算小了。真正高质量的贡献，不是“改得多”，而是：

- 理解边界
- 改动克制
- 验证到位
- 文档同步
`,
  "docs/ARTICLE_QUERY_API": `# 文章列表查询 API 说明

基础路径前缀以部署为准，默认为 **\`/api/v1/wx\`**。

## 认证

以下接口需要登录后的 **JWT**（\`Authorization: Bearer <token>\`）或 **API Key**：

- 请求头：\`X-API-Key: werss_xxxx\`
- 或：\`Authorization: Bearer werss_xxxx\`（当值中不含 \`.\` 时按 API Key 处理，与 JWT 区分）

API Key 在 Web 端 **「API Key 管理」** 页面创建。

## 获取文章列表

\`GET\` 或 \`POST\` **\`/api/v1/wx/articles\`**

### 分页与排序

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| \`offset\` | int | 0 | 偏移量，≥0 |
| \`limit\` | int | 5 | 每页条数，1～100 |

列表按 **\`publish_time\` 降序**（新文章在前）。

### 基础筛选

| 参数 | 类型 | 说明 |
|------|------|------|
| \`status\` | string | 文章状态；不传则排除已删除 |
| \`search\` | string | 标题关键词，空格/\`|\`/\`-\` 拆成多词，**OR** 匹配 |
| \`mp_id\` | string | 仅某公众号 |
| \`has_content\` | bool | \`true\` 时只查含 \`content\` 字段的完整记录 |

### 发布时间（\`publish_time\` 存库为毫秒时间戳）

| 参数 | 类型 | 说明 |
|------|------|------|
| \`publish_from\` | int | 起始时间（含）。若数值 **&lt; 10¹²** 视为**秒**，否则视为**毫秒** |
| \`publish_to\` | int | 结束时间（含）。规则同上 |

可与下面「日期」参数同时使用：取**更严**的区间（下界取较大、上界取较小）。

### 按日历日筛选（UTC）

| 参数 | 类型 | 说明 |
|------|------|------|
| \`publish_date_from\` | string | \`YYYY-MM-DD\`，该日 00:00:00 UTC（含） |
| \`publish_date_to\` | string | \`YYYY-MM-DD\`，该日 23:59:59.999 UTC（含） |

### 标签筛选

| 参数 | 类型 | 说明 |
|------|------|------|
| \`tag_id\` | string | 单个标签 ID |
| \`tag_ids\` | string | 多个标签 ID，**逗号分隔** |
| \`tag_match\` | string | \`any\`（默认）：命中**任一**标签即可；\`all\`：必须同时包含**全部**所列标签 |

标签 ID 与 **\`GET /api/v1/wx/tags\`** 返回的 \`id\` 一致。

### 响应

\`\`\`json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [ /* 文章对象，含 mp_name、mp_cover、tags、tag_names */ ],
    "total": 123
  }
}
\`\`\`

### 示例

\`\`\`bash
# JWT
curl -s -H "Authorization: Bearer YOUR_JWT" \\
  "http://localhost:8001/api/v1/wx/articles?limit=20&offset=0&mp_id=MP_WXS_xxx&search=AI"

# API Key + 日期 + 标签（任一命中）
curl -s -H "X-API-Key: werss_your_key" \\
  "http://localhost:8001/api/v1/wx/articles?limit=10&publish_date_from=2026-03-01&publish_date_to=2026-03-20&tag_ids=id1,id2&tag_match=any"
\`\`\`

### 缓存

相同查询条件结果可能缓存约 **5 分钟**（内存缓存）；筛选参数均参与缓存键，更新后稍候即可看到最新数据或重启服务。

---

更多全局说明见 [README.md](../README.md) 中的「API 文档」与 Swagger：\`/api/docs\`。
`,
  "docs/ui/wechat-mp-management": `# 公众号管理

公众号管理页是一个更直接的管理视角。和“订阅管理”相比，它更像一个基础数据维护页，而不是日常采集操作页。

## 1. 访问路径

- \`/wechat/mp\`

通常需要 \`wechat:manage\` 相关权限。

## 2. 这页和订阅管理有什么区别

两者都围绕公众号，但侧重点不同：

- 订阅管理
  更偏日常使用、查看详情、刷新文章
- 公众号管理
  更偏基础记录维护和增删改

当前代码里，这个页面实际上仍然复用了订阅数据源，并没有另一套完全独立的 \`mp\` 管理后端。

## 3. 页面结构

### 列表区

当前页面展示的是一个表格，字段主要包括：

- 公众号 ID
- 名称
- 状态
- 最后同步时间
- 操作按钮

它不是“左列表右详情”的页，而是标准表格管理页。

### 操作区

每行支持：

- 编辑
- 删除

页面顶部支持：

- 添加公众号

## 4. 新增和编辑

弹窗表单主要字段包括：

- 公众号 ID
- 名称
- 封面图
- 简介
- 状态

新增时，本质上还是走创建订阅的逻辑；
编辑时，则走更新订阅的逻辑。

所以这里要理解一个现实：

- “公众号管理页”目前更多是管理 \`Feed\`/订阅记录的另一种 UI 入口
- 不是一个完全独立的公众号实体后台

## 5. 删除时要注意什么

当前页面删除调用的是删除订阅逻辑。

因此不能在文档里直接写成“删除公众号一定会删除所有相关文章”，因为这取决于后端删除实现，不能凭页面字面推断。

更稳妥的理解是：

- 它会删除该公众号对应的订阅记录
- 相关文章如何处理，要以实际后端删除逻辑为准

## 6. 适合什么场景

这页更适合：

- 管理员批量维护公众号基础信息
- 纠正名称、封面图、简介
- 快速禁用某个来源

不太适合：

- 做日常内容处理
- 观察某个订阅最近文章详情

这些更适合在订阅管理页做。

## 7. 常见问题

### 为什么这里和订阅管理的数据很像

因为当前实现本来就复用了订阅接口和数据。

### 为什么没有采集历史区

因为当前页面代码没有独立的采集历史面板。旧文档里那种“右侧详情 + 采集历史”写法并不符合现状。

### 为什么上传头像只看到 URL

页面当前支持上传封面图并把返回 URL 写回表单，本质上仍然是资源地址管理，不是复杂媒体库。

## 8. 建议怎么用

- 要看和刷文章：去 [订阅管理](./subscriptions.md)
- 要做基础信息维护：用这页

这样职责最清楚。
`,
  "docs/ui/tags": `# 标签管理页面

## 面向谁

需要为文章打标签、对公众号内容进行人工分类的操作员。

## 解决什么问题

手动创建标签、管理已有标签的启用状态、删除不需要的标签，或通过 CSV 批量导入导出标签。

## 标签列表页

**访问路径：** \`/tags\`

### 页面元素

| 区域 | 内容 |
|------|------|
| 页面标题 | 标签管理 |
| 搜索框 | 按标签名称搜索，支持实时过滤 |
| 标签列表 | 展示标签名称、状态（启用/禁用/封禁）、文章数量、创建时间 |
| 操作列 | RSS 链接、编辑、删除按钮 |

### 如何理解"文章数量"

\`文章数量\`是该标签关联的文章总数（通过 \`article_tags\` 表统计）。显示为徽章样式，如 \`12\`。

点击标签名称旁边的 **RSS 图标**（\`</>Rss\`），可以在新标签页打开该标签的 RSS Feed，格式为 \`/feed/tag/{tag_id}.atom\`。

### 批量操作

- 勾选左侧复选框，可一次性删除多个标签
- 顶部会显示已选择数量，点击"批量删除"确认

### 状态切换

点击状态徽章（\`启用\` / \`禁用\` / \`封禁\`）可以直接切换状态：
- **绿色 "启用"**：标签正常使用
- **灰色 "禁用"**：标签暂时不可用
- **红色 "封禁"**：标签被锁定，不可切换

### 导出 / 导入

页面右上角提供"导出标签"和"导入标签"按钮：

- **导出**：生成 \`标签列表.csv\` 文件，包含所有标签的 id、名称、封面图、描述、状态、创建时间、关联公众号 ID
- **导入**：上传 CSV 文件，支持增量更新（id 相同则更新，不存在则新建）

## 标签编辑页

**访问路径：** \`/tags/add\`（新建）、\`/tags/edit/{id}\`（编辑）

### 可编辑的字段

| 字段 | 说明 |
|------|------|
| 标签名称 | 必填，2-30 个字符 |
| 封面图 | 上传图片作为标签封面，建议尺寸 120x120，支持 JPG/PNG，最大 2MB |
| 描述 | 标签的简介说明 |
| 状态 | 启用 / 禁用 / 封禁 |
| 关联公众号 | 可选择该标签关联哪些公众号（留空表示关联所有） |

### 表单提交

- 新建标签后需要手动刷新（点击"应用"按钮）任务才会生效
- 编辑后同样需要刷新

## 筛选功能

列表页搜索框支持实时过滤，输入关键词后列表会自动刷新到第 1 页。点击"清除"可恢复显示全部标签。

## 分页

页面底部显示总条数和当前页码，支持跳转到指定页。默认每页 10 条。

## 常见操作流程

**新建标签：**
1. 点击"添加标签"
2. 填写标签名称、封面、描述
3. 选择关联公众号（可选）
4. 点击"提交"
5. 返回列表页，点击"应用"按钮使改动生效

**批量导入标签：**
1. 在列表页点击"导入标签"
2. 选择 CSV 文件（需包含 \`标签名称\`、\`状态\`、\`mps_id\` 列）
3. 系统显示导入结果（导入数、更新数、跳过数）

**导出后编辑再导入：**
1. 点击"导出标签"获取 \`标签列表.csv\`
2. 在 Excel 中编辑（如修改描述、状态）
3. 点击"导入标签"上传修改后的文件
`,
  "docs/ui/tag-clusters": `# 标签聚类页面

## 面向谁

需要分析标签之间语义关系、发现潜在标签合并机会的运营人员。

## 解决什么问题

通过聚类算法将相似标签分组，查看每个聚类的成员标签和代表性标签（中心标签），并获取标签合并建议。

## 标签聚类列表页

**访问路径：** \`/tag-clusters\`

### 页面元素

| 区域 | 内容 |
|------|------|
| 页面标题 | 标签聚类 |
| 刷新按钮 | 重新加载聚类列表 |
| 重新分析按钮 | 触发 AI 重新聚类（耗时较长，弹窗显示进度） |
| 搜索框 | 按聚类名称、描述、中心标签 ID 搜索 |
| 聚类列表 | 展示聚类名称、描述、中心标签 ID、成员数量、版本号 |

### 列表字段说明

| 字段 | 说明 |
|------|------|
| 名称 | 聚类的名称，通常来自中心标签名称 |
| 描述 | 聚类的语义描述 |
| 中心标签 | 聚类的核心标签 ID，代表该组的主题 |
| 成员数量 | 该聚类包含的标签数量 |
| 版本号 | 聚类分析的版本，用于追踪更新 |
| 操作 | "查看" 按钮进入详情页 |

### 重新分析

点击"重新分析"后，AI 会对所有标签重新进行语义聚类，过程中显示弹窗提示（无法取消）。完成后自动刷新列表。

## 标签聚类详情页

**访问路径：** \`/tag-clusters/{id}\`

### 三个 Tab

#### 1. 概览（Overview）

展示聚类的基本信息：

| 字段 | 说明 |
|------|------|
| 聚类名称 | 该聚类的名称 |
| 成员数量 | 包含的标签数 |
| 中心标签 | 代表性标签名称和 ID |

下方有两个表格：

**成员标签列表：**

| 字段 | 说明 |
|------|------|
| 标签名称 | 成员标签的名称 |
| 标签 ID | 成员标签的 ID |
| 成员得分 | 该标签在聚类中的归属分数（分数越高越属于这个聚类） |

**合并建议（Merge Suggestions）：**

| 字段 | 说明 |
|------|------|
| 源标签 | 建议被合并的标签 |
| 目标标签 | 建议合并到的目标标签 |
| 总分 | 合并的语义相似度得分 |
| 原因 | 合并建议的理由说明 |

合并建议帮助运营者判断哪些标签语义重复，可以合并为同一个。

#### 2. 散点图（Scatter）

将聚类中的标签以向量形式投影到二维平面，支持 PCA / t-SNE / UMAP 三种降维方法。可调整参数：

| 参数 | 说明 |
|------|------|
| 降维方法 | PCA / t-SNE / UMAP |
| 包含边 | 是否显示标签之间的关系边 |
| 最小边权重 | 过滤低相似度的边 |
| 归一化 | 是否对向量做归一化处理 |

点击某个点会弹出提示显示标签名称。

#### 3. 网络图（Network）

展示聚类内标签的共现关系网络。节点大小反映成员得分，边的粗细反映标签间的相似度。

可用滑块调整"最小相似度阈值"，过滤掉相似度低于阈值的边，更清晰地展示核心关系。

同样支持点击节点查看标签名称。

## 理解 Merge Suggestion

Merge Suggestion 是 AI 分析后给出的标签合并建议：

- **源标签**（source_tag）：语义上可以被合并的标签
- **目标标签**（target_tag）：建议合并到的目标
- **总分**（score）：两个标签之间的语义相似度得分（原始值 / 10000 保留两位小数）
- **原因**（reason）：给出该建议的语义依据

运营者可以根据建议，手动将源标签的内容迁移到目标标签，然后删除源标签。

## 导出

详情页右上角提供"导出 JSON"按钮，将该聚类的完整信息（成员列表、合并建议、描述等）下载为 \`tag-cluster-{id}.json\` 文件。
`,
  "docs/ui/sys-info": `# 系统信息

系统信息页用于查看当前实例的运行状态、资源占用、微信登录状态、队列状态和任务调度状态。它更偏运维和排障，不是日常内容操作页。

## 1. 访问路径

- \`/sys-info\`

通常需要管理员权限。

## 2. 这个页面解决什么问题

当你遇到下面这些问题时，系统信息页通常是第一批排查入口：

- 后台任务没跑
- 微信登录好像失效了
- 文章补全队列卡住了
- 系统资源紧张
- 当前版本是否落后

## 3. 页面结构

当前页面主要包括 3 类信息。

### 系统资源

展示系统资源占用，例如：

- CPU
- 内存
- 磁盘
- 队列信息

资源卡片主要来自系统资源接口和单独的资源组件。

### 文章统计

如果接口返回了文章统计信息，会显示：

- 公众号总数
- 文章总数
- 无正文数量
- 有正文数量
- 已删除或异常数量

这部分很适合判断：

- 采集是否在正常积累数据
- 正文补全是否跟得上

### 系统与运行状态

包括：

- 操作系统信息
- Python 版本
- 系统运行时间
- API 版本
- 核心版本
- 最新版本
- 微信 token / 登录状态
- 队列状态

## 4. 页面里的几个关键信号

### 微信登录状态

如果微信未登录或登录即将失效，很多采集动作都会失败。出现采集异常时，先看这里，不要先查前端。

### 队列状态

它能帮助判断后台是否还有待处理任务，例如补全文、文章同步等。

### 最新版本 / 是否需要更新

这更多是运维提示，不代表必须立刻升级，但能帮助你识别当前实例是否明显落后。

## 5. 和设置页的关系

设置页更偏“控制”，系统信息页更偏“观测”。

可以这样理解：

- 设置页告诉你“系统应该怎么跑”
- 系统信息页告诉你“系统现在实际怎么跑”

两者要结合看，尤其是在任务异常时。

## 6. 常见排查路径

### 任务没跑

先看：

- 微信是否登录
- 队列是否卡住
- 调度器是否运行

### 文章少但订阅很多

先看：

- 文章统计
- 无正文数量
- 微信状态

### 页面能开但系统很慢

先看：

- 资源使用
- 队列积压

## 7. 适合谁看

- 管理员
- 部署与维护人员
- 排查线上问题的开发者
`,
  "docs/ui/subscriptions": `# 订阅管理页

## 1. 页面作用

订阅管理页用于管理已添加的微信公众号订阅。用户可以查看订阅列表、查看订阅详情、刷新订阅（抓取最新文章）、编辑订阅信息、删除订阅，以及添加新订阅。

## 2. 入口路径

- 路径：\`/subscriptions\`
- 导航栏点击"订阅管理"进入

## 3. 页面结构

页面采用**左右布局**：

### 3.1 左侧：订阅列表

- **搜索框**：按公众号名称搜索
- **添加按钮**：跳转 \`/add-subscription\` 添加新订阅
- **订阅表格**：显示公众号名称、文章数量、状态
  - 点击行可选中该订阅，右侧显示详情
  - 选中行有高亮（左侧 4px 边框 + 浅色背景）
- **分页**：底部分页导航，支持跳转页

### 3.2 右侧：订阅详情

选中订阅后显示：

- **头部**：公众号头像、名称、ID、操作按钮
- **信息卡片**：
  - 状态（已启用/已禁用）
  - 文章数量
  - 最后同步时间
  - 时间跨度（最早文章 ~ 最新文章）
  - 简介
  - RSS 地址（有则显示）

- **操作按钮**：
  - **刷新**：立即抓取该订阅的最新文章（\`/feed/mp/{mp_id}\`）
  - **编辑**：弹出编辑弹窗
  - **删除**：确认后删除

### 3.3 空态

未选中订阅时，右侧显示"请从左侧列表选择一个订阅查看详情"。

## 4. 常见操作

### 4.1 刷新订阅

点击右侧"刷新"按钮，向后端发起抓取请求。后端调用微信采集接口，抓取该公众号的最新文章。

**限制**：同一订阅 60 秒内只能刷新一次。刷新过于频繁会返回 40402 错误，提示剩余等待时间。

### 4.2 查看订阅详情

点击订阅行，右侧显示该订阅的完整信息：
- 头像、名称、ID
- 状态、文章数、同步时间
- 简介
- RSS 链接（可点击跳转）

### 4.3 编辑订阅

点击"编辑"按钮，弹出 Dialog：
- 修改公众号名称
- 修改简介
- 切换启用/禁用状态
- ID 不可修改（只读）

### 4.4 删除订阅

点击"删除"按钮，弹出确认框。确认后删除订阅及其所有文章。

### 4.5 添加新订阅

点击左上角"添加订阅"按钮，跳转 \`/add-subscription\` 页面。

## 5. 注意事项

- 刷新操作有 60 秒防抖限制，防止频繁请求
- 删除订阅会级联删除该订阅下的所有文章
- RSS 地址由后端动态生成，格式为 \`/feed/mp/{mp_id}.rss\`
- 订阅状态为 \`status=1\` 表示启用，\`status=0\` 表示禁用
- 左侧列表选中状态通过 URL 参数同步（如 \`/subscriptions/{mp_id}\`）

## 6. 常见报错 / 空态

| 场景 | 表现 |
|---|---|
| 无订阅 | 左侧列表显示"暂无数据" |
| 刷新过于频繁 | Toast 提示"请等待 X 秒后再试" |
| 刷新失败 | Toast 显示错误信息 |
| 删除失败 | Toast 显示错误信息 |

## 7. 关联功能

- **添加订阅**（\`/add-subscription\`）：通过文章链接提取公众号、手动输入、导入/导出 CSV
- **文章列表**（\`/articles\`）：按公众号筛选文章
- **RSS**：\`/feed/mp/{mp_id}.{format}\` 获取该订阅的 RSS Feed
- **消息任务**：可以为订阅配置定时抓取任务，通过 \`mps_id\` 指定订阅
`,
  "docs/ui/settings": `# 设置页

## 1. 页面作用

设置页面向普通用户，提供应用偏好设置和部分系统配置的可视化入口。用户无需修改配置文件，即可在 Web UI 中调整行为配置。

## 2. 入口路径

- 路径：\`/settings\`
- 导航栏点击"设置"进入

## 3. 页面结构

页面按功能分为多个卡片分组：

### 3.1 应用设置卡片

| 配置项 | 说明 |
|---|---|
| 显示水印 | 开关，在页面上显示版权水印信息 |
| 暗色模式 | 开关，切换暗色/亮色主题 |
| 采集数据起始时间 | 日期选择器，设置公众号文章采集的起始日期，系统从该日期开始抓取 |

### 3.2 文章定期清理卡片

控制是否自动删除旧文章（需配合 \`server.enable_job=true\` 和 \`-job True\` 启动参数）：

| 配置项 | 说明 |
|---|---|
| 启用定期清理 | 总开关 |
| 保留最近 | 数字输入，保留多少天内的文章 |
| 时间基准 | 下拉选择：\`created_at\`（入库时间）或 \`publish_time\`（微信发布时间） |

**注意**：
- 默认保留最近 7 天
- 删除方式与 \`article.true_delete\` 配置一致
- 若设置 \`WERSS_ENV_OVERRIDES_DB=true\`，则未在环境变量 / yaml 中留空的项才会使用此处写入数据库的值

### 3.3 图片与对象存储卡片

控制是否将公众号文章图片转存到 MinIO：

| 配置项 | 说明 |
|---|---|
| 转存公众号图片到 MinIO | 总开关 |

三种状态：
- **MinIO 未启用或不可用**：不转存，使用微信侧图片链接
- **MinIO 可用、开关关闭**：不转存，节省存储但外链可用性受上游访问策略影响
- **MinIO 可用、开关开启**：下载并上传到对象存储，使用存储 URL（更稳定）

### 3.4 标签提取设置卡片

**位置**：设置页最下方区域（定时抓取任务卡片上方）

管理 AI 自动提取标签的提示词模板：

| 配置项 | 说明 |
|---|---|
| 标签提取提示词 | Textarea，支持占位符 \`{{max_tags}}\`、\`{{custom_tags_hint}}\`、\`{{text}}\` |
| 预设模板 | 下拉选择：科技、医学、法律、自定义 |

**预设说明**：
- **科技**：默认提示词，优先提取公司名、产品名、技术名词
- **医学**：优先提取疾病名、药物名、治疗方案、科室
- **法律**：优先提取法条名、案由、程序节点、机构名
- **自定义**：用户自行编辑的提示词

保存后直接影响后续标签提取结果。

### 3.5 定时抓取任务卡片

显示调度器状态和消息任务列表：

| 区域 | 说明 |
|---|---|
| 调度器状态 | 显示"运行中"（绿色）或"未运行"（灰色），包含任务数量 |
| 定时任务列表 | 显示每个消息任务的名称、状态（已启用/已禁用）、Cron 表达式、下次执行时间 |
| 刷新状态按钮 | 重新加载调度器信息 |

**前提条件**：需服务以 \`-job True\` 启动且 \`server.enable_job=true\`，调度器才会运行。

## 4. 常见操作

### 4.1 修改采集起始时间

1. 点击日期选择器，选择日期
2. 点击"保存"按钮
3. 系统将从该日期开始采集文章（不影响已采集的历史文章）

### 4.2 配置文章清理策略

1. 开启"启用定期清理"开关
2. 设置保留天数（如 7 天）
3. 选择时间基准
4. 点击"保存"

### 4.3 修改标签提取提示词

1. 从预设模板中选择（如"医学"）
2. 或选择"自定义"后直接编辑 Textarea
3. 点击"保存提示词"按钮

### 4.4 刷新调度器状态

点击"刷新状态"按钮，重新获取调度器运行状态和消息任务下次执行时间。

## 5. 注意事项

- 设置页中保存的配置项（除水印、暗色模式外）均写入数据库 \`config_management\` 表
- 若 \`WERSS_ENV_OVERRIDES_DB=true\`，则以环境变量和 \`config.yaml\` 为准，数据库值仅在对应键未设置或为空时生效
- 标签提取提示词保存后只影响新提交的文章，不追溯已提取的标签
- 文章定期清理需要同时满足：开关开启 + \`-job True\` 启动 + \`server.enable_job=true\`

## 6. 常见报错 / 空态

| 场景 | 表现 |
|---|---|
| 保存失败 | Toast 显示错误信息 |
| 标签提取提示词为空 | 阻止保存，提示"标签提取提示词不能为空" |
| 调度器未运行 | 状态显示"未运行"，任务列表显示"暂无定时抓取任务" |
| 保留天数 < 1 | 阻止保存，提示"保留天数至少为 1" |

## 7. 关联功能

- **标签提取**（\`/articles\`）：设置页的提示词配置影响文章列表页的标签提取
- **消息任务**（\`/message-tasks\`）：消息任务在设置页的调度器区域显示状态
- **RSS 与消息任务文档**：\`docs/features/rss-and-message-tasks.md\`
`,
  "docs/ui/message-tasks": `# 消息任务页面

消息任务页用于把“文章数据”主动推送出去。它不是内容生产页，而是自动分发页，适合把公众号更新定时发到企业微信、钉钉、飞书或其他 Webhook 接收端。

## 1. 访问路径

- 列表页：\`/message-tasks\`
- 新建页：\`/message-tasks/add\`
- 编辑页：\`/message-tasks/edit/{id}\`

## 2. 列表页能做什么

当前列表页支持：

- 查看任务列表
- 查看 cron 表达式的人类可读解释
- 创建任务
- 编辑任务
- 删除任务
- 测试执行
- 正式执行
- 刷新调度器
- 开启浏览器通知

## 3. 列表页几个关键按钮

### 应用

这个按钮非常重要。它不是“刷新页面”按钮，而是触发任务刷新，让新的任务配置真正进入调度器。

如果你：

- 新建了任务
- 编辑了 cron
- 改了启用状态

但没有点“应用”，后台未必会按新配置执行。

### 测试

测试执行的目标是：

- 验证模板能不能渲染
- 验证 Webhook 是否可用

它和正式执行的区别在于不会走完整采集流程。

### 执行

正式执行会尝试：

- 按任务配置处理公众号
- 更新文章
- 再发送消息

所以它更像“完整跑一遍任务”。

## 4. 浏览器通知是做什么的

列表页有浏览器通知开关。它属于页面增强能力，不影响后端任务本身。

开启后：

- 浏览器可以在检测到新文章时做标题闪烁或提示

不开启也不会影响后台消息任务正常跑。

## 5. 表单页字段怎么理解

当前表单的关键字段有：

- 任务名称
- 类型
- 消息模板
- WebHook 地址
- cron 表达式
- 公众号范围
- 状态

### 类型

当前主要有两种：

- \`Message\`
- \`WebHook\`

它们的核心差异在于模板和发送方式。

### 消息模板

页面内置了示例模板按钮。

当前逻辑会根据选中的公众号数量，自动给出两类示例：

- 单公众号模板
- 多公众号聚合模板

如果选择的是 \`WebHook\` 类型，还会给出 JSON 风格的示例模板。

### 公众号范围

可留空，也可多选。

留空通常表示：

- 对所有公众号生效

选择后则只处理指定公众号。

### cron 表达式

页面支持用选择器辅助填写，不要求手敲所有表达式。

## 6. 模板变量怎么理解

消息模板不是只替换一个标题字段，而是围绕“文章集合”和“公众号集合”渲染。

页面里的示例模板已经说明了两种典型上下文：

- 单公众号 + 多篇文章
- 多公众号聚合 + 每个公众号多篇文章

因此写模板时不要只假设有一个 \`title\` 变量，要按任务作用范围来写。

## 7. 一个正确的使用流程

如果你要建一个每日推送任务，正确顺序是：

1. 新建任务
2. 填任务名称
3. 选类型
4. 选 cron
5. 选公众号范围
6. 用示例模板起步
7. 提交保存
8. 回列表页点“应用”

最后这一步不能漏。

## 8. 常见问题

### 为啥任务保存了但没跑

优先检查：

- 是否点了“应用”
- 后端是否以 \`-job True\` 启动
- 任务状态是否启用
- cron 是否正确

### 测试成功但正式执行不对

说明模板和 Webhook 大概率没问题，优先查：

- 文章更新流程
- 公众号范围配置
- 微信登录状态

### 列表里显示 \`WeekHook\`

这是当前页面里的文案拼写问题，不影响功能本身，它实际表达的是 \`WebHook\` 类型。

## 9. 适合谁看

- 需要主动推送内容的管理员
- 负责企业微信群 / 钉钉群内容分发的人
- 要修改消息任务前后端逻辑的开发者
`,
  "docs/ui/hot-topics": `# 热点发现页面

热点发现页用于把最近几天的文章聚合成“事件型热点”，帮助用户快速知道最近这些公众号集中在讨论什么。

## 1. 访问路径

- \`/hot-topics\`

## 2. 这个页面解决什么问题

它不是关键词排行榜，也不是标签页的变体。它的目标是：

- 从最近几天的文章中识别主题
- 把相近文章归成一个热点
- 给出摘要、信号词和代表性文章

适合用来回答：

- 最近科技圈在集中聊什么
- 哪几个话题值得继续跟
- 哪些文章其实在讲同一个事件

## 3. 页面结构

### 顶部操作区

页面顶部有两个主要动作：

- 刷新
- 重新分析

### 分析信息卡

如果系统里已经有最近一次分析结果，顶部会展示：

- 分析时间
- 时间窗口
- 热点数量
- 使用模型

### 热点列表

每个热点会展示：

- 排名
- 话题名称
- 摘要
- 相关文章数量
- 信号词

并支持展开“相关文章”区域。

## 4. 刷新和重新分析的区别

### 刷新

只重新拉取后端已经存在的热点结果，不触发新的分析。

### 重新分析

会真正触发后端重新跑一次热点发现流程。

当前页面在重新分析时会：

- 弹出进行中的对话框
- 显示已用时间
- 阻止误关闭对话框
- 完成后自动重新加载数据

所以它不是一个瞬时按钮，而是一个可能运行几分钟的任务触发器。

## 5. 话题里的几个字段怎么理解

### 话题名称

表示这个聚类出来的事件或主题，不应该只是公司名。

### 摘要

是对该热点的简要说明，帮助用户快速理解“为什么这些文章会被归到一起”。

### 信号词

信号词是支撑这个热点的关键词或短语，但它们不是“页面最终想让你看的东西”。

真正重要的是：

- 这些信号词共同指向了哪个事件

### 代表性文章

展开后可以看到这个热点下的代表性标题列表，用于快速判断：

- AI 聚得对不对
- 这个主题值不值得继续跟

## 6. 页面更适合怎么用

### 每日查看

每天打开一次，看最近 3 天形成了哪些新主题。

### 选题参考

如果你要做内容选题，这页比直接刷文章列表更高效，因为它已经先做了“归堆”。

### 结果校验

如果你在调热点算法，这页就是最直观的验证面板。

## 7. 和标签页的区别

标签页回答的是：

- 文章都有哪些标签

热点页回答的是：

- 最近几篇文章其实在说同一件什么事

所以热点页更接近“事件发现”，标签页更接近“内容结构化”。

## 8. 常见问题

### 页面为空

通常是：

- 还没有成功跑过热点分析
- 最近没有可用文章
- 热点任务失败了

### 重新分析很慢

这是正常的，因为它需要拉取文章、调用模型并存储结果。

### 看起来像关键词榜

如果你看到的结果只剩下大公司名，说明当前分析质量不够理想，应该回头看热点发现逻辑，而不是页面本身。

## 9. 适合谁看

- 内容运营
- 科技信息整理人员
- 调整热点发现算法的开发者
`,
  "docs/ui/dashboard": `# 仪表板

仪表板页用于给用户一个全局概览。它不是系统主工作台，但适合快速判断“系统最近有没有在正常工作”。

## 1. 访问路径

- \`/dashboard\`

## 2. 适合谁看

- 想快速看系统整体数据的人
- 管理员或运营人员
- 需要确认近期采集和标签趋势的人

## 3. 这个页面解决什么问题

用户不需要先进入文章列表、订阅页、标签页分别查看，而是在一个页面上先获得这些全局信号：

- 文章总量
- 订阅总量
- 今日新增
- 近一周新增
- 文章来源分布
- 热门标签或关键词趋势

## 4. 页面结构

当前页面主要由三部分组成：

### 统计卡片

展示系统当前最重要的几个总量指标。

典型包括：

- 总文章数
- 总订阅数
- 今日新增
- 近 7 天新增

### 图表区

当前仪表板引入了较重的图表库，并做了延迟加载与错误兜底。图表主要用于展示：

- 来源分布
- 关键词分布
- 标签趋势

这也是为什么仪表板在实现上比一般“卡片页”复杂。

### 近期趋势与统计明细

让用户看到过去一段时间的数据变化，而不是只看静态总数。

## 5. 数据来源

优先来源：

- 仪表板专用接口 \`getDashboardStats\`

回退来源：

- 文章列表接口
- 订阅列表接口

当前实现里，如果专用接口失败，会退回到前端自己拉文章和订阅数据后做本地统计。这意味着：

- 页面能更稳地展示
- 但在数据量大时，回退逻辑会更重

## 6. 使用时怎么理解这些数字

### 今日新增

表示今天新进入系统的文章量，用于看采集是否在工作。

### 总订阅数

表示系统当前管理的公众号来源数量。

### 关键词趋势

它更多是“内容信号”而不是严格的数据指标。适合观察近期关注点，不适合当精确分析结论。

## 7. 常见问题

### 图表加载失败

当前页面有单独的错误边界和懒加载兜底。如果看到“图表加载失败”，通常是图表库加载异常，而不是整个仪表板接口都挂了。

### 数字和列表页不完全一致

可能原因：

- 仪表板走的是聚合统计接口
- 或者走了回退统计逻辑
- 某些页面默认隐藏了 AI 过滤文章

## 8. 和其他页面的关系

- 想看细节：去文章列表页
- 想改来源：去订阅管理页
- 想看标签整理：去标签页
- 想看热点事件：去热点发现页

所以仪表板更像“概览页”，不是操作页。
`,
  "docs/ui/articles": `# 文章列表页

## 1. 页面作用

文章列表页是 WeRSS 的默认首页（\`/\` 或 \`/articles\`），是整个系统的"工作台"。用户在此完成文章的搜索、筛选、查看、批量操作、标签管理和 RSS 跳转等核心操作。

## 2. 入口路径

- 默认首页：\`/\`
- 显式路径：\`/articles\`
- 导航栏点击"文章"进入

## 3. 页面结构

页面分为三个区域：

### 3.1 筛选区（Filter Card）

位于页面顶部，包含：

- **关键词搜索**：输入框，支持按标题关键词搜索（多词 OR 匹配）
- **公众号筛选**：下拉框，选择"全部"或某个具体公众号
- **重置按钮**：清空筛选条件

筛选区下方还有一个**操作条**，包含：
- **隐藏 AI 筛选文章开关**：默认开启，隐藏 \`ai_filter_status\` 为 \`hide\` 的文章
- **AI 增强操作组**：文章过滤、标签提取、批量删除等操作入口

### 3.2 列表区（Main Table）

表格列：

| 列 | 说明 |
|---|---|
| 复选框 | 勾选文章，支持批量操作 |
| 标题 | 文章标题，点击可打开文章详情抽屉（Drawer） |
| 来源 | 公众号名称 |
| 标签 | AI 提取的标签，显示最多 3 个，超出显示 \`+N\` |
| 过滤标签 | AI 筛选状态：\`keep\`（保留）、\`maybe\`（待定）、\`hide\`（隐藏）。无状态显示 \`—\` |
| 发布时间 | 微信文章发布时间 |
| 操作 | 恢复隐藏文章（仅 \`hide\` 状态显示）、删除 |

**过滤标签与普通标签的显示差异：**

- **普通标签**（\`tag_names\` / \`topic_names\`）：以 \`outline\` 样式 Badge 显示，位于"标签"列，最多显示 3 个
- **过滤标签**（\`ai_filter_status\`）：以不同颜色 Badge 显示，位于"过滤标签"列：
  - \`keep\`：默认 outline 样式，表示保留
  - \`maybe\`：secondary 样式，表示待定
  - \`hide\`：destructive 样式，表示隐藏

### 3.3 批量操作区

位于列表区顶部右侧或操作条中，包括：
- 选中 N 篇文章后显示"已选 N 篇"
- 批量删除按钮（红色）
- AI 过滤/标签提取等批量操作下拉菜单

### 3.4 RSS 跳转区

页面右上角，包含"RSS"下拉按钮，支持生成当前筛选条件下的 RSS Feed：
- **ATOM**：Atom 格式
- **RSS**：RSS 2.0 格式
- **JSON**：JSON Feed 格式

链接规则：
- 选中公众号时：\`/feed/mp/{mp_id}.{format}\`
- 未选公众号（全部）：\`/feed/all.{format}\`

## 4. 常见操作

### 4.1 查看文章详情

点击文章标题，打开右侧抽屉（Drawer），显示：
- 文章标题、来源、发布时间
- 原文链接、上下篇导航
- 文章正文内容（已处理图片 URL）

### 4.2 切换文章状态

点击标题下方的状态 Badge（绿色=已发布，灰色=草稿），可切换 \`status\` 在 1（启用）和 2（禁用）之间切换。

### 4.3 AI 过滤

对文章进行 AI 二分类筛选（保留/隐藏）：

- **过滤选中**：对勾选的文章进行过滤
- **过滤当前页**：对当前页所有文章进行过滤
- **过滤未过滤**：对尚未进行 AI 筛选的文章进行过滤
- **过滤全库**：对数据库中所有文章进行过滤（需二次确认）

过滤过程中可暂停、继续、停止。完成后显示：隐藏数、保留数、待定数。

### 4.4 恢复隐藏文章

对于 \`ai_filter_status\` 为 \`hide\` 的文章，操作列显示"恢复"按钮，点击可恢复为 \`keep\` 状态。

### 4.5 标签提取

批量提取或重新提取文章标签：

- **提取选中**：对勾选的文章重新提取标签
- **提取当前页**：对当前页所有文章提取标签
- **补充缺失标签**：仅对尚未提取标签的文章提取（异步任务，有进度条）
- **提取全库**：对所有文章重新提取（需二次确认）

### 4.6 导出

点击右上角"导出"按钮，弹出导出配置弹窗，可导出选中文章或当前筛选结果。

## 5. 注意事项

- AI 过滤和标签提取为后台任务，中断后可在刷新页面后从 localStorage 恢复进度
- 删除为物理删除（\`article.true_delete=true\`），不可恢复
- 文章详情中的图片 URL 可能被处理过（添加 \`/static/res/logo/\` 前缀）
- \`hideAiFiltered\` 开关仅影响显示，不影响数据库

## 6. 常见报错 / 空态

| 场景 | 表现 |
|---|---|
| 无文章 | 表格显示"暂无数据" |
| 筛选无结果 | 表格显示"暂无数据"，检查筛选条件 |
| 标签提取失败 | Toast 提示错误信息，可重试 |
| AI 过滤中断 | 进度保存在 localStorage，刷新后可恢复 |
| 文章详情加载失败 | Toast 提示错误，详情抽屉关闭 |

## 7. 关联功能

- **RSS 输出**：\`/feed/mp/{mp_id}.{format}\` 或 \`/feed/all.{format}\`
- **标签管理**：标签在文章列表页提取，汇总到标签管理页面
- **AI 筛选**：筛选结果影响文章显示，过滤标签状态存储在 \`article_ai_filters\` 表
- **消息任务**：RSS 是被动的信息拉取，消息任务是主动的通知推送
- **导出**：文章可导出为 CSV/PDF/Markdown
`,
  "docs/ui/api-keys": `# API 密钥管理

API Key 管理页用于给系统用户创建和维护可编程访问凭证。它面向的是“要让脚本、自动化流程或第三方程序访问 WeRSS API”的场景，不是给普通阅读用户用的。

## 1. 访问路径

- \`/api-keys\`

通常需要管理员权限或相应访问权限。

## 2. 这个页面能做什么

当前页面支持：

- 查看当前用户的 API Key 列表
- 创建新的 API Key
- 编辑名称、权限和启用状态
- 删除 API Key
- 重新生成 Key
- 查看使用日志

## 3. 页面结构

页面通常分为左右或上下两个主要区域：

### Key 列表

展示：

- 名称
- 权限
- 启用状态
- 最近使用时间
- 创建时间

### 详情与日志

选中某个 Key 后，可查看：

- 基础信息
- 使用日志
- 是否显示真实 Key 值

## 4. 权限字段怎么理解

当前前端里权限选项比较轻，主要是：

- \`read\`
- \`read_write\`

但后端实际保存的是字符串形式的权限字段，所以这里本质上是一个“权限串管理”页面，而不是完整 RBAC 控制台。

## 5. 创建时要注意什么

最重要的一点：

- API Key 的明文通常只在创建或重生成时完整返回一次

页面当前为了解决“看完就丢”的问题，会把新生成的 Key 通过本地存储暂时保留映射关系，用于当前浏览器后续显示。

这意味着：

- 换浏览器或清空本地存储后，旧明文不一定还能看见
- 但 Key 本身仍然有效，后端不会因为前端看不见而失效

## 6. 使用日志怎么看

API Key 日志主要用于回答这几个问题：

- 这个 Key 是否真的被调用过
- 调的是哪个接口
- 请求方法是什么
- 返回状态码是否异常

如果出现“某个自动化脚本说自己在调用，但系统没反应”，这页是第一批排查入口。

## 7. 常见操作建议

### 给内部脚本使用

建议单独创建一个名称明确的 Key，不要复用个人临时 Key。

### 给不同用途拆 Key

建议按用途拆开，例如：

- 只读同步
- 运维脚本
- 外部集成

这样日志和权限更清楚。

### 怀疑泄漏时

最稳的是直接重新生成或删除旧 Key，而不是继续复用。

## 8. 常见误区

### 误区一：创建完随时都能在页面重新看到完整 Key

不一定。页面只是做了本地缓存辅助展示，后端不会反复返回完整明文。

### 误区二：API Key 和登录 JWT 是一回事

不是。JWT 是登录态，API Key 是长期可编程凭证。

### 误区三：只有管理员才有意义

不完全对。管理员更常用，但普通用户如果系统允许，也可能需要为自己的自动化脚本创建 Key。
`,
  "docs/ui/add-subscription": `# 添加订阅

添加订阅页是把新公众号引入 WeRSS 的入口页。它既能手动新增，也能通过文章链接反查公众号信息，还集成了订阅列表的导入导出。

## 1. 访问路径

- \`/add-subscription\`

## 2. 这个页面能做什么

当前页面实际支持 4 类动作：

- 通过公众号关键字搜索候选公众号
- 通过微信文章链接提取公众号信息
- 手动填写并创建订阅
- 导出 / 导入订阅列表 CSV

它不是多标签页复杂表单，而是一个单页表单 + 两个辅助动作。

## 3. 页面结构

### 顶部操作区

页面头部通常有这几个按钮：

- 通过文章获取公众号信息
- 导出订阅列表
- 导入订阅列表

这里最有用的是“通过文章获取公众号信息”，因为它能减少手动输入错误。

### 主表单

主表单字段包括：

- 公众号名称
- 公众号 ID / 微信侧标识
- 头像地址
- 简介

页面会实时预览头像。

### 自动补全搜索

页面里还有一个搜索候选的自动补全能力：

- 输入关键字后调用公众号搜索接口
- 选择候选项后回填名称、ID、简介、头像

## 4. 最常用的操作方式

### 方式一：通过文章链接反查

这是最稳的一种：

1. 点“通过文章获取公众号信息”
2. 粘贴微信文章链接
3. 系统提取公众号信息
4. 自动回填到表单
5. 确认后提交

适合你已经看到了某篇文章，但系统里还没这个公众号的场景。

### 方式二：搜索后回填

如果你只有公众号名的模糊记忆：

1. 在搜索框里输入关键字
2. 从候选里选中目标公众号
3. 表单自动回填
4. 提交创建

### 方式三：完全手动录入

当搜索或文章提取都拿不到时，可以手动填：

- 名称
- 公众号 ID
- 头像 URL
- 简介

这种方式灵活，但最容易录错。

## 5. 导入导出怎么理解

页面上带了订阅导入导出功能，但当前实现是 CSV 方向，不是文档里常见那种“OPML 批量导入页”。

### 导出

点击导出后，会下载订阅列表 CSV。

### 导入

点击导入后，会选择本地 CSV 文件上传，并调用后端导入。

导入成功后，页面会提示：

- 新增数量
- 更新数量
- 跳过数量

然后跳回订阅管理页。

## 6. 提交成功后会发生什么

提交本页表单后：

1. 系统创建订阅记录
2. 跳回首页或订阅管理主链路
3. 后续由手动刷新或定时任务开始真正抓文章

也就是说：

- 添加订阅不等于立刻采到文章
- 文章还要靠后续采集链路补上

## 7. 常见问题

### 链接提取失败

优先检查：

- 是否真的是微信文章链接
- 链接是否完整
- 当前微信采集相关能力是否正常

### 选了搜索结果但字段不完整

因为候选结果本身可能只返回部分字段，必要时仍需手动修正。

### 添加成功后看不到文章

这是正常的。订阅创建只完成“建源”，采集还没发生或尚未完成。

## 8. 和其他页面的关系

- 添加完后，去 [订阅管理](./subscriptions.md) 看详情和手动刷新
- 文章真正进来后，去 [文章列表](./articles.md) 看结果

## 9. 当前实现边界

这页当前并没有真正做这些事：

- 多种标签页式批量导入流程
- OPML 导入流程
- 复杂的导入预览页

如果后面要补这些能力，应该基于当前代码再扩，不要按旧文档想象继续写。
`,
  "docs/features/tag-system": `# 标签系统

## 1. 功能目标

为文章打标签，实现文章分类、筛选和聚合。标签是热点发现、标签聚类的前置数据。

## 2. 适用场景

- 用户手动管理标签，对文章进行分类
- AI 自动从文章内容中提取标签
- 按标签筛选文章列表
- 标签聚类前需要先有标签数据

## 3. 核心对象

| 对象 | 说明 |
|------|------|
| \`Tags\` | 标签实体，\`tags\` 表 |
| \`ArticleTag\` | 文章-标签关联，\`article_tags\` 表 |
| \`is_custom\` | 区分手动标签与 AI 标签（\`is_custom=True\` 为手动） |

## 4. 数据流 / 工作流程

\`\`\`
文章 → AI 提取标签 → Tags 表
      ↓
手动创建标签 → Tags 表
      ↓
标签关联文章 → ArticleTag 表
      ↓
标签筛选 / 标签聚类 / 热点发现
\`\`\`

### 4.1 AI 提取标签

- 触发：\`core/tag_extractor.py\` 的 \`TagExtractor.extract_with_ai()\`
- 调用 OpenAI 兼容 API（配置项 \`openai.api_key\` / \`openai.base_url\` / \`openai.model\`）
- Prompt 模板中优先匹配用户自定义标签（\`is_custom=True\`）
- 配置项：\`article_tag.max_tags\`（默认 5）、\`article_tag.ai_prompt\`（自定义 prompt）

### 4.2 手工标签与 AI 标签的关系

- **手工标签：** \`Tags.is_custom = True\`，用户在 \`/tags\` 页面手动创建
- **AI 标签：** 系统通过 LLM 自动提取，不标记 \`is_custom\`
- 两者共存于同一张 \`tags\` 表，通过 \`is_custom\` 字段区分
- 手工标签会被缓存到 TagExtractor 中，用于 AI 提取时的优先匹配

## 5. 主要能力

### 5.1 标签管理（CRUD）

- API：\`POST /api/v1/wx/tags\`（创建）、\`GET /api/v1/wx/tags\`（列表）
- API：\`PUT /api/v1/wx/tags/{tag_id}\`（更新）、\`DELETE /api/v1/wx/tags/{tag_id}\`（删除）
- 批量删除：\`DELETE /api/v1/wx/tags?tag_ids=id1,id2\`

### 5.2 文章标签关联

- API：\`POST /api/v1/wx/article-tag\`（关联标签到文章）
- API：\`GET /api/v1/wx/article-tag?article_id=xxx\`（获取文章的所有标签）
- API：\`DELETE /api/v1/wx/article-tag/{tag_id}?article_id=xxx\`（删除关联）

### 5.3 标签筛选

- 文章列表 API 支持 \`tag_id\` / \`tag_ids\` 筛选
- 筛选标签通过 \`is_filter\` 字段区分（与普通标签存在同一张表）

### 5.4 标签聚类（见 \`docs/features/tag-clusters.md\`）

- 标签 → 标签聚类，是独立功能，不在本节讨论范围

## 6. 关键配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| \`openai.api_key\` | AI API Key | - |
| \`openai.base_url\` | API Base URL | https://api.openai.com/v1 |
| \`openai.model\` | 模型名称 | gpt-4o |
| \`article_tag.max_tags\` | 每次提取标签数 | 5 |
| \`article_tag.ai_prompt\` | 自定义 prompt 模板 | 内置默认模板 |

## 7. 关联页面与 API

- **Web UI：** \`/tags\`（标签列表）、文章详情页（标签展示）
- **API：**
  - \`GET /api/v1/wx/tags\` - 标签列表（分页，含近 3 天文章数）
  - \`POST /api/v1/wx/tags\` - 创建标签
  - \`PUT /api/v1/wx/tags/{tag_id}\` - 更新标签
  - \`DELETE /api/v1/wx/tags/{tag_id}\` - 删除标签
  - \`POST /api/v1/wx/tags/test/extract\` - 测试 AI 提取
  - \`POST /api/v1/wx/article-tag\` - 为文章添加标签
  - \`GET /api/v1/wx/article-tag\` - 获取文章的所有标签

## 8. 常见问题 / 限制

- **AI 提取失败：** API Key 未配置或网络问题会导致提取失败，返回空列表
- **标签同名冲突：** 标签名称相同时系统不会去重，需业务层处理
- **删除标签：** 删除标签时自动清除 ArticleTag 关联记录

## 9. 相关文件

\`\`\`
core/tag_extractor.py          # 标签提取逻辑
core/models/tags.py           # Tags 模型
core/models/article_tags.py  # ArticleTag 模型
apis/tags.py                 # 标签管理 API
apis/article_tag.py          # 文章-标签关联 API
web_ui/src/views/TagList.tsx  # 标签列表页面
\`\`\`
`,
  "docs/features/subscription-management": `# 订阅管理

订阅管理是 WeRSS 的数据源入口。系统后面的大部分能力都建立在“先有公众号订阅，再有文章采集”这条链路上。

## 1. 这个功能真正管理的是什么

虽然页面叫“订阅管理”，但当前代码里它的核心实体实际上是 \`Feed\`，也就是公众号在系统中的订阅表示。

它不是泛化意义上的 RSS 订阅器，而是：

- 保存公众号基础信息
- 保存采集关联标识
- 作为文章抓取的入口
- 作为 RSS 输出的上游实体

## 2. 相关页面和接口

前端页面：

- [web_ui/src/views/SubscriptionManagement.tsx](../../web_ui/src/views/SubscriptionManagement.tsx)
- [web_ui/src/views/AddSubscription.tsx](../../web_ui/src/views/AddSubscription.tsx)

后端接口：

- [apis/mps.py](../../apis/mps.py)
- [apis/export.py](../../apis/export.py)

核心模型：

- [core/models/feed.py](../../core/models/feed.py)

## 3. 订阅管理承担的能力

### 公众号搜索

用户可以根据公众号名称或关键字搜索公众号。搜索不是查本地库，而是通过微信侧能力去查候选公众号。

### 添加订阅

当前支持的思路包括：

- 通过文章链接提取公众号信息
- 手动输入公众号信息
- 直接搜索后添加

### 查看订阅详情

当前订阅管理页是典型的“左列表 + 右详情”结构。选中一个订阅后，可看到：

- 公众号头像
- 名称和 ID
- 简介
- 状态
- 文章数量
- 最近文章时间范围

### 手动刷新文章

这是订阅管理最有价值的动作之一。它会直接调用更新接口，对指定公众号执行一次抓取。

注意：

- 有频率限制
- 依赖微信登录状态
- 不是简单刷新前端缓存

### 编辑和删除

支持修改公众号基础信息和状态，也支持删除订阅。

删除时要意识到一个事实：

- 订阅删除不等于所有历史文章一定立刻完全消失
- 还要考虑文章表和导出层是否有历史数据

## 4. 订阅到文章的真实链路

这条链路是理解项目的关键：

1. 用户添加订阅
2. 系统生成 \`Feed\` 记录
3. 记录 \`faker_id\` 等抓取关联信息
4. 定时或手动触发采集
5. 文章进入 \`Article\` 表
6. 后续才有标签、热点、RSS、消息任务

所以“订阅管理”并不是个孤立后台页，它决定了整个系统的上游质量。

## 5. 频繁遇到的几个误区

### 误区一：订阅只是给 RSS 用的

不对。RSS 只是订阅的一个输出形态，订阅本身是采集入口。

### 误区二：刷新订阅只是前端刷新

不对。当前的“刷新”会触发后端抓取逻辑，属于真实业务动作。

### 误区三：公众号名就是唯一标识

不对。当前系统里真正参与抓取和关联的还有：

- \`id\`
- \`faker_id\`

不能只看展示名。

## 6. 手动刷新时后台做了什么

在 [apis/mps.py](../../apis/mps.py) 里，手动刷新至少会做这些检查：

- 订阅是否存在
- 距离上次更新是否过近
- 微信 Session 是否有效
- 启动抓取线程
- 把新文章通过回调写入数据库

所以当用户说“点刷新报错”，不要先怀疑前端。优先查：

- 登录状态
- 限流提示
- 微信 token 是否过期

## 7. 和导入导出的关系

订阅管理还和导入导出打通了：

- 可导出公众号列表为 CSV
- 可导出为 OPML
- 可从 CSV 导入

这让它不仅是操作页，也是迁移和备份入口。

## 8. 谁应该重点看这篇文档

- 想搞清楚系统数据入口的开发者
- 负责日常运营和维护订阅池的使用者
- 要修改抓取入口、订阅详情或导入导出的人
`,
  "docs/features/settings-and-ops": `# 设置与运维

设置页不是一个简单的“个性化偏好”页面，而是 WeRSS 的运行控制台。它混合了前端偏好、后端配置覆盖、调度器状态和运维相关开关。

## 1. 这块功能到底管什么

当前“设置与运维”主要涵盖 4 类内容：

- 前端显示偏好
- 采集和标签相关配置
- 文章存储与清理策略
- 调度器和消息任务的运行状态

这也是为什么当前设置页会显得比普通后台更重：它不是单纯 UI 设置，而是部分运行控制面板。

## 2. 相关页面和接口

前端页面：

- [web_ui/src/views/Settings.tsx](../../web_ui/src/views/Settings.tsx)

相关接口：

- [apis/config_management.py](../../apis/config_management.py)
- [apis/sys_info.py](../../apis/sys_info.py)

配置基础：

- [docs/dev/configuration-and-precedence.md](../dev/configuration-and-precedence.md)

## 3. 当前设置页包含哪些区域

### 应用偏好

这部分更偏前端本地设置，例如：

- 主题模式
- 水印开关

这类配置通常不会写回系统核心配置，只影响当前浏览体验。

### 采集起始时间

这是一个非常关键的业务配置。它会影响系统估算抓取页数和历史回补范围。

在任务侧，\`jobs/mps.py\` 会读取 \`collect_start_date\`，据此估算要抓多少页。

所以这个字段不是展示配置，而是真正影响采集行为。

### 标签提取 Prompt

设置页里现在维护了多套 Prompt 预设：

- 科技
- 医学
- 法律
- 自定义

对当前项目来说，这一块非常重要，因为标签提取质量直接影响：

- 标签列表质量
- 标签聚类效果
- 热点发现辅助信号

### 文章图片转存

这部分控制 MinIO 可用时是否转存公众号文章图片。

它不是纯前端行为，而是对象存储策略开关。

### 文章保留策略

设置页支持控制：

- 是否启用定期清理
- 保留天数
- 以 \`created_at\` 还是 \`publish_time\` 作为基准

这部分最终会影响后台清理任务注册和实际删除逻辑。

### 调度器与消息任务状态

设置页会读取系统信息接口，展示：

- 采集调度器状态
- 补全文调度器状态
- 已配置消息任务

这让它具备了运维视角，而不只是配置表单。

## 4. 配置优先级必须怎么理解

这块最容易被写错，也最容易让使用者误解。

不要简单说“页面保存后就生效”。真实情况是：

- \`config.example.yaml\` 只是示例
- \`config.yaml\` 是文件配置
- \`.env\` 是环境变量源
- 数据库 \`config_management\` 是控制台覆盖源
- 运行时环境变量可能再覆盖一次

具体优先级请看 [配置与优先级](../dev/configuration-and-precedence.md)。

在文档里必须强调：

- 设置页写入的不是唯一配置源
- 某些配置可能被环境变量覆盖

## 5. 为什么它属于“运维”

因为这页已经不只是给普通用户调偏好的页面了。它还承担了这些运维判断：

- 调度器有没有运行
- 消息任务是否已经生效
- 文章保留策略是否会自动清理数据
- MinIO 图片策略是否开启
- 当前 Prompt 配置是否正确

如果线上出现“任务不跑”“清理不生效”“图片没转存”，设置页和系统信息页是第一批要看的地方。

## 6. 常见误区

### 误区一：保存配置后所有行为都会立刻变

不对。取决于配置类型：

- 前端本地设置通常立即生效
- 后端配置可能需要重新读取
- 调度器相关配置有时需要重新注册任务

### 误区二：设置页等于配置文件编辑器

不对。它是“控制台覆盖层”，不是完整替代 \`config.yaml\`。

### 误区三：这里都是低风险改动

不对。文章清理、抓取起始时间、Prompt、对象存储开关都可能直接影响生产行为。

## 7. 谁应该优先看这篇

- 管理员
- 负责部署和日常维护的人
- 要扩展控制台设置能力的前后端开发者
`,
  "docs/features/rss-and-message-tasks": `# RSS 与消息任务

## 1. 功能目标

RSS 和消息任务是 WeRSS 的两种信息推送机制：

- **RSS**：被动拉取，用户通过 RSS Reader 订阅公众号的 Feed，系统按需生成 XML/JSON
- **消息任务**：主动推送，用户配置 Webhook 和消息模板，系统按 Cron 表达式定时发送文章摘要到指定渠道

两者都围绕"文章数据"展开，但面向不同使用场景。

## 2. 适用场景

| 场景 | 使用 RSS | 使用消息任务 |
|---|---|---|
| 用户自行用 RSS Reader 订阅 | ✓ | |
| 定时推送到企业微信/钉钉/飞书群 | | ✓ |
| 按公众号隔离推送不同渠道 | | ✓ |
| 支持全文/摘要切换 | ✓ | ✓（模板控制） |
| 需要登录认证 | 可选 | 需要（消息任务需管理员配置） |

## 3. 核心对象

### 3.1 Feed（公众号订阅）

\`\`\`python
class Feed:
    id: str          # 公众号 ID（mp_id）
    mp_name: str     # 公众号名称
    mp_intro: str   # 公众号简介
    mp_cover: str   # 公众号封面图
    status: int      # 1=启用，0=禁用
\`\`\`

Feed 是 RSS 的基础单位，也是消息任务中指定推送范围的单位。

### 3.2 MessageTask（消息任务）

\`\`\`python
class MessageTask:
    id: str              # 任务 ID（UUID）
    name: str             # 任务名称
    message_template: str  # 消息模板（支持占位符）
    web_hook_url: str    # Webhook 地址
    message_type: int    # 0=article, 其他扩展
    mps_id: str          # 关联的公众号 ID 列表（JSON 数组字符串）
    cron_exp: str        # Cron 表达式
    status: int          # 1=启用，0=禁用
\`\`\`

## 4. RSS 工作流程

### 4.1 路由入口

\`\`\`
GET /feed/mp/{mp_id}.rss   → RSS 2.0
GET /feed/mp/{mp_id}.atom  → ATOM
GET /feed/mp/{mp_id}.json  → JSON Feed
GET /feed/all.rss           → 全部订阅的 RSS（未指定 mp_id）
\`\`\`

### 4.2 生成流程

\`\`\`
请求到达 apis/rss.py get_mp_articles_source()
    │
    ├── 查询 Feed + Article 表
    │   - 指定 mp_id：仅该公众号文章
    │   - 无 mp_id：所有公众号文章
    │   - 支持 tag_id 筛选（按标签关联的订阅）
    │
    ├── 构建 rss_list：
    │   - id: 文章 ID
    │   - title: 文章标题
    │   - link: 文章链接（local=True 时为代理链接，否则为微信原文）
    │   - description: 摘要
    │   - content: 正文（full_context=True 时包含）
    │   - image: 封面图
    │   - mp_name: 公众号名称
    │   - updated: 发布时间
    │
    ├── 缓存文章内容到 data/cache/content/{article_id}.json
    │
    └── core/rss.py RSS.generate() 生成对应格式
        - rss/atom: XML
        - json: JSON
        - 支持 cdata、add_cover 等配置
\`\`\`

### 4.3 RSS 配置

\`config.yaml\` 中的 RSS 相关配置：

\`\`\`yaml
rss:
  base_url: \${RSS_BASE_URL:-}   # 填写真实域名，RSS 链接才正确
  local: \${RSS_LOCAL:-False}    # True=本地代理，False=微信原文
  full_context: \${RSS_FULL_CONTEXT:-False}  # True=全文；保守默认False，仅输出摘要
  add_cover: \${RSS_ADD_COVER:-False}  # True=添加封面 enclosure；保守默认False
  cdata: \${RSS_CDATA:-False}    # True=内容用 CDATA
  page_size: \${RSS_PAGE_SIZE:-10}  # 每页条数
\`\`\`

### 4.4 本地代理模式

当 \`rss.local=True\` 时，文章链接指向 \`\${base_url}/rss/content/{article_id}\`，该端点返回处理后的 HTML 页面（添加 \`/static/res/logo/\` 前缀到图片 URL）。

## 5. 消息任务工作流程

### 5.1 路由入口

消息任务通过 Web UI 管理（\`/message-tasks\`），API 在 \`apis/message_task.py\`：

\`\`\`
GET    /message_tasks           → 列表
POST   /message_tasks           → 创建
PUT    /message_tasks/{id}      → 更新
DELETE /message_tasks/{id}     → 删除
POST   /message_tasks/{id}/run → 手动触发
GET    /message_tasks/{id}     → 详情
\`\`\`

### 5.2 定时执行流程

\`\`\`
APScheduler 触发 jobs/taskmsg.py
    │
    ├── 读取 MessageTask 表，筛选 status=1 的任务
    │
    ├── 对每个任务：
    │   ├── 解析 mps_id（JSON 数组），获取对应的公众号列表
    │   │
    │   ├── 查询这些公众号的最新文章
    │   │
    │   ├── 渲染 message_template
    │   │   支持占位符：{{title}}、{{link}}、{{description}}、{{content}} 等
    │   │
    │   └── 调用 core/notice/notice()
    │           │
    │           ├── 识别 Webhook 类型（企业微信/钉钉/飞书/custom）
    │           │
    │           └── 调用对应发送函数
    │               - send_wechat_message()
    │               - send_dingtalk_message()
    │               - send_feishu_message()
    │               - send_custom_message()
\`\`\`

### 5.3 message_template 占位符

模板中可使用以下占位符（由 \`core/content_format.py\` 的 \`format_content\` 处理）：

| 占位符 | 说明 |
|---|---|
| \`{{title}}\` | 文章标题 |
| \`{{link}}\` | 文章链接 |
| \`{{description}}\` | 文章摘要 |
| \`{{content}}\` | 文章正文（HTML） |
| \`{{mp_name}}\` | 公众号名称 |
| \`{{publish_time}}\` | 发布时间 |

### 5.4 Webhook 类型自动识别

\`core/notice/__init__.py notice()\` 函数根据 Webhook URL 自动识别通知类型：

| Webhook URL 特征 | 类型 |
|---|---|
| \`qyapi.weixin.qq.com\` | 企业微信 |
| \`oapi.dingtalk.com\` | 钉钉 |
| \`open.feishu.\` 或 \`feishu.cn\` | 飞书 |
| 其他 | custom（直接 POST） |

## 6. RSS 与消息任务的边界与联系

### 6.1 边界

| 维度 | RSS | 消息任务 |
|---|---|---|
| 触发方式 | 用户主动拉取（RSS Reader） | 系统主动推送（Webhook） |
| 频次控制 | 无限制，用户自己控制 | 按 Cron 表达式，最小 1 分钟 |
| 目标用户 | 分散的 RSS Reader 用户 | 集中在企业群/通知渠道 |
| 内容控制 | 默认摘要，可显式开启全文 | 完全自定义模板 |
| 认证 | 可选（代码中被注释） | 需要管理员登录 |

### 6.2 联系

- 数据源相同：都从 \`articles\` 表查询数据
- 可按公众号隔离：RSS 按 \`mp_id\`，消息任务按 \`mps_id\`
- 都可以输出全文（RSS 用 \`full_context\`，消息任务模板中用 \`{{content}}\`），但公开或多人分发前应确认内容授权和使用边界

## 7. 关键配置

### 7.1 Webhook 内容格式

\`\`\`yaml
webhook:
  content_format: \${WEBHOOK.CONTENT_FORMAT:-html}  # html/text/markdown
\`\`\`

### 7.2 通知渠道配置

\`\`\`yaml
notice:
  dingding: "\${DINGDING_WEBHOOK}"
  wechat: "\${WECHAT_WEBHOOK}"
  feishu: "\${FEISHU_WEBHOOK}"
  custom: "\${CUSTOM_WEBHOOK}"
\`\`\`

## 8. 关联页面与 API

| 资源 | 页面 | API |
|---|---|---|
| RSS Feed | 文章列表页右上角 RSS 按钮 | \`GET /feed/mp/{mp_id}.{format}\` |
| 消息任务 | \`/message-tasks\` | \`apis/message_task.py\` |
| 消息任务执行 | 消息任务页"测试"/"执行"按钮 | \`POST /message_tasks/{id}/run\` |
| 调度器状态 | 设置页"定时抓取任务"卡片 | \`GET /api/sys_info\` |

## 9. 常见问题

### 9.1 RSS 链接打不开

检查 \`config.yaml\` 中 \`rss.base_url\` 是否为真实可访问的域名。本地开发时默认为请求的 \`base_url\`。

### 9.2 消息任务不执行

排查：
1. 调度器是否运行（设置页查看"调度器状态"）
2. 任务 \`status\` 是否为 1（启用）
3. \`cron_exp\` 是否正确
4. 进程是否以 \`-job True\` 启动

### 9.3 Webhook 发送失败

检查：
1. Webhook URL 是否正确
2. 网络是否能访问对应域名（企业微信/钉钉/飞书）
3. \`notice_type\` 是否与 URL 匹配
4. 消息内容是否超限（部分渠道有大小限制）

## 10. 相关文件

- \`apis/rss.py\` — RSS 路由
- \`apis/message_task.py\` — 消息任务 CRUD
- \`core/rss.py\` — RSS 生成器
- \`core/notice/__init__.py\` — 通知调度器
- \`core/notice/wechat.py\`、\`dingtalk.py\`、\`feishu.py\`、\`custom.py\` — 各渠道实现
- \`jobs/taskmsg.py\` — 定时任务执行逻辑
- \`web_ui/src/views/MessageTaskList.tsx\` — 消息任务页面
- \`web_ui/src/views/ArticleListPage.tsx\` — RSS 按钮入口
`,
  "docs/features/hot-topics": `# 热点发现

## 1. 功能目标

从最近 N 天的科技文章中自动发现热点主题，将多篇相关文章聚合为一个热点话题，供用户了解当前科技趋势。

## 2. 适用场景

- 了解最近几天科技领域发生了哪些重要事件
- 从文章聚合的角度发现主题，而非按标签聚合
- 每天早上自动运行，用户查看当日热点简报

## 3. 核心对象

| 对象 | 说明 |
|------|------|
| \`HotTopicRun\` | 热点发现任务的执行记录 |
| \`HotTopic\` | 热点主题实体 |
| \`HotTopicArticle\` | 热点主题与文章的关联 |

## 4. 数据流 / 工作流程

\`\`\`
定时任务触发（每天 9:00，可配置） 或 手动触发
        ↓
从 Article 表拉取近 N 天文章（带标签）
        ↓
调用 LLM（OpenAI 兼容 API）对文章进行事件级聚类
        ↓
解析 LLM 返回结果，存储 HotTopic + HotTopicArticle
        ↓
用户在 /hot-topics 页面查看热点列表
\`\`\`

### 4.1 时间窗口

- 默认 3 天（配置项 \`hot_topics.window_days\`）
- 可通过 \`hot_topics.cron\` 配置定时任务触发时间（默认 \`0 9 * * *\`）

### 4.2 与标签聚类的区别

| | 标签聚类（Tag Cluster） | 热点发现（Hot Topic） |
|---|---|---|
| **聚合对象** | 标签（Tag） | 文章（Article） |
| **聚合依据** | embedding 相似度 + n-gram 相似度 | 文章内容/语义相关性 |
| **目的** | 将相近的标签标准化 | 发现最近发生的事件主题 |
| **数据来源** | 所有标签 | 近期文章 |

## 5. 主要能力

### 5.1 手动触发

- Web UI：点击 \`/hot-topics\` 页面的"重新分析"按钮
- API：\`POST /api/v1/wx/hot-topics/rebuild\`（**需要登录**，\`Authorization: Bearer <access_token>\`）

### 5.2 查看历史结果

- API：\`GET /api/v1/wx/hot-topics/recent\` - 获取最近一次成功的热点发现结果（**无需登录**）
- 包含：\`run_id\`, \`window_days\`, \`model_name\`, \`created_at\`, \`topics[]\`
- 每个 topic 包含：\`id\`, \`topic_name\`, \`summary\`, \`signals\`, \`article_count\`, \`article_ids\`, \`representative_titles\`
- 指定某次 run 详情：\`GET /api/v1/wx/hot-topics/runs/{run_id}\`（**需要登录**）

### 5.3 定时任务

- 由 \`jobs/hot_topics_job.py\` 在 \`start_all_task()\` 中注册到 \`TaskScheduler\`（与采集等任务同一调度器）
- 进程需以 **\`main.py -job True\` 且配置 \`server.enable_job: true\`** 启动，才会在 [web.py 生命周期](../../web.py) 里拉起定时任务线程；否则**不会**自动跑 cron，仅可依赖手动触发
- 默认每天 9:00 执行（\`hot_topics.cron\`，默认 \`0 9 * * *\`）
- 需要配置 \`openai.api_key\`（或环境变量 \`OPENAI_API_KEY\`）；未配置时 **不会注册** 热点定时任务（仅警告日志）

## 6. 关键配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| \`hot_topics.enabled\` | 是否启用热点发现 | true |
| \`hot_topics.cron\` | 定时任务 cron 表达式 | \`0 9 * * *\` |
| \`hot_topics.window_days\` | 时间窗口（天数） | 3 |
| \`openai.api_key\` | AI API Key | - |
| \`openai.base_url\` | API Base URL | https://api.openai.com/v1 |
| \`openai.model\` | 模型名称 | gpt-4o |

## 7. 关联页面与 API

- **Web UI：** \`/hot-topics\`（热点发现页面）
- **API：**
  - \`GET /api/v1/wx/hot-topics/recent\` - 获取最近热点结果
  - \`GET /api/v1/wx/hot-topics/skill-brief\` - **供外接撰写智能体 / Skill**：在最新一次成功 run 上，**每个**热点附带前 N 篇（默认 3 篇）参考文章，含标题、链接、公众号名与**可选正文**（**需登录**；Query：\`articles_per_topic\`、\`include_content\`、\`max_content_chars\`）
  - \`POST /api/v1/wx/hot-topics/rebuild\` - 手动触发热点发现
  - \`GET /api/v1/wx/hot-topics/runs/{run_id}\` - 获取指定 run 详情

### 7.1 每日拉取后送给撰写 Skill 的推荐流程

1. 保证当天已有成功热点：依赖 **定时任务**（\`-job True\` + \`hot_topics.cron\`）或定时调用 **\`POST /rebuild\`**。  
2. 定时（如 cron）调用 **\`GET /api/v1/wx/hot-topics/skill-brief?articles_per_topic=3\`**，带登录 Token。  
3. 解析 \`data.topics[]\`：每个元素含 \`topic_name\`、\`summary\`、\`signals\`、\`reference_articles\`（3 篇的 \`title\`/\`url\`/\`content\` 等），整段 JSON 交给你侧的文章生成 Skill/流水线。  
4. 若正文过大，可加 **\`max_content_chars=8000\`** 或设 **\`include_content=false\`** 仅用标题+链接。

仓库内示例脚本见 **\`scripts/fetch_hot_topics_skill_brief.sh\`**（需自行填入 \`BASE_URL\`、账号密码或已有 Token）。


## 8. 常见问题 / 限制

- **无文章数据：** 若近 N 天没有文章，热点发现返回空结果
- **API Key 未配置：** 定时任务注册时会警告，但不会报错
- **执行时间较长：** LLM 调用和解析可能需要几分钟，前端有倒计时展示

## 9. 相关文件

\`\`\`
core/hot_topics/service.py      # 热点发现核心逻辑
core/hot_topics/prompt.py      # LLM 聚类 prompt
core/hot_topics/parser.py      # LLM 返回结果解析
core/hot_topics/repository.py  # 数据库操作
core/models/hot_topics.py      # HotTopic 模型
core/models/hot_topic_runs.py  # HotTopicRun 模型
core/models/hot_topic_articles.py # HotTopicArticle 模型
jobs/hot_topics_job.py         # 定时任务注册
apis/hot_topics.py            # API 路由
web_ui/src/views/HotTopics.tsx # 热点发现页面
\`\`\`
`,
  "docs/features/export-and-storage": `# 导出与存储

## 1. 功能目标

导出与存储模块负责将 WeRSS 中的数据（公众号、标签）导出为标准格式文件，并管理文章中图片的存储策略。

## 2. 适用场景

- 将公众号列表导出为 CSV 或 OPML，供备份或迁移
- 将标签列表导出为 CSV，供批量编辑后重新导入
- 文章图片从微信服务器转存到 MinIO，避免图片失效
- 公众号头像统一上传到 MinIO，保证展示一致性

## 3. 核心对象

| 对象 | 说明 |
|------|------|
| \`Feed\` | 公众号订阅，\`feeds\` 表，一条记录对应一个公众号 |
| \`Tags\` | 标签，\`tags\` 表 |
| \`Article\` | 文章，\`articles\` 表 |
| MinIO Bucket | 对象存储空间，存储图片和头像 |

## 4. 数据流 / 工作流程

### 4.1 公众号导出

\`\`\`
用户请求 /export/mps/export
    │
    ├── 查询 Feed 表，按关键词筛选
    │
    ├── 构建 CSV 数据（id, 公众号名称, 封面图, 简介, 状态, 创建时间, faker_id）
    │
    ├── 创建临时 CSV 文件（temp_mp_export.csv）
    │
    └── 返回 FileResponse，浏览器下载完成后自动删除临时文件
\`\`\`

**支持的导出格式：**

- **CSV**：适合 Excel 编辑和数据分析
- **OPML**：标准的 RSS 订阅列表格式，可导入其他 RSS 阅读器

\`\`\`
GET /export/mps/export        → CSV 文件下载
GET /export/mps/opml          → OPML 文件下载
\`\`\`

### 4.2 标签导出 / 导入

\`\`\`
导出：GET /export/tags
    ├── 查询 Tags 表
    ├── 生成 CSV（id, 标签名称, 封面图, 描述, 状态, 创建时间, mps_id）
    └── 返回下载

导入：POST /export/tags/import
    ├── 验证 CSV 必要列（标签名称、状态、mps_id）
    ├── 遍历每行：
    │   ├── 根据 id 查找是否已存在
    │   ├── 存在则更新，不存在则创建
    │   └── 跳过空名称的行
    └── 返回统计（导入数、更新数、跳过数）
\`\`\`

### 4.3 图片转存 MinIO

当文章从微信抓取后，系统可以将正文中引用的图片转存到 MinIO：

\`\`\`
文章抓取完成
    │
    ├── 检查 minio.enabled 和 minio.store_article_images 配置
    │
    ├── 对每张图片：
    │   ├── 下载原始图片到内存
    │   ├── 用 URL 的 MD5 哈希作为文件名（避免重复）
    │   ├── 上传到 MinIO bucket: articles/{article_id}/{filename}
    │   └── 替换文章内容中的图片 URL 为 MinIO 地址
    │
    └── 头像处理类似：avatars/{mp_id}/{filename}
\`\`\`

**存储路径规则：**

| 资源类型 | MinIO 中的路径 |
|----------|----------------|
| 文章图片 | \`articles/{article_id}/{md5hash}.{ext}\` |
| 公众号头像 | \`avatars/{clean_mp_id}/{md5hash}.{ext}\` |

**MinIO 访问方式：**

- **公开 Bucket**：\`{public_url}/{bucket}/{object_name}\` 或 \`{endpoint}/{bucket}/{object_name}\`
- **私有 Bucket**（\`use_presigned_url=True\`）：生成 7 天有效的 presigned URL

## 5. 主要能力

### 5.1 公众号导入 / 导出

| API | 方法 | 说明 |
|-----|------|------|
| \`/export/mps/export\` | GET | 导出公众号 CSV |
| \`/export/mps/import\` | POST | 导入公众号 CSV（支持增量更新） |
| \`/export/mps/opml\` | GET | 导出公众号 OPML |

### 5.2 标签导入 / 导出

| API | 方法 | 说明 |
|-----|------|------|
| \`/export/tags\` | GET | 导出标签 CSV |
| \`/export/tags/import\` | POST | 导入标签 CSV（支持增量更新） |

### 5.3 图片转存

| 函数 | 说明 |
|------|------|
| \`MinIOClient.upload_image()\` | 下载并上传文章图片到 MinIO |
| \`MinIOClient.upload_avatar()\` | 下载并上传公众号头像到 MinIO |
| \`should_mirror_article_images()\` | 业务开关，判断是否启用转存 |

## 6. 关键配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| \`minio.enabled\` | 是否启用 MinIO | \`False\` |
| \`minio.endpoint\` | MinIO 服务地址（如 \`minio.example.com:9000\`） | - |
| \`minio.access_key\` | Access Key | - |
| \`minio.secret_key\` | Secret Key | - |
| \`minio.bucket\` | Bucket 名称 | \`articles\` |
| \`minio.public_url\` | 公开访问的基础 URL | - |
| \`minio.secure\` | 是否使用 HTTPS | \`False\` |
| \`minio.use_presigned_url\` | 是否使用 presigned URL（私有 bucket） | \`False\` |
| \`minio.store_article_images\` | 是否转存文章图片，建议仅在有授权或私有归档需求时开启 | \`False\` |

## 7. 关联页面与 API

| 资源 | 页面 | API |
|------|------|-----|
| 公众号导出 | 公众号列表页右上角"导出"按钮 | \`GET /export/mps/export\` |
| 公众号导入 | 公众号列表页右上角"导入"按钮 | \`POST /export/mps/import\` |
| 标签导出 | 标签列表页右上角"导出标签"按钮 | \`GET /export/tags\` |
| 标签导入 | 标签列表页右上角"导入标签"按钮 | \`POST /export/tags/import\` |
| OPML 导出 | （无独立页面，通过 URL 访问） | \`GET /export/mps/opml\` |

## 8. 常见问题 / 限制

- **导入 CSV 格式错误**：缺少必要列（\`标签名称\`、\`状态\`、\`mps_id\`）时返回 400 错误
- **导入标签名称为空**：自动跳过该行，不影响其他行
- **MinIO 未安装**：\`pip install minio\`，未安装时图片转存功能不可用，控制台会输出警告
- **MinIO 配置不完整**：即使 \`minio.enabled=True\`，配置不完整时客户端不会初始化
- **图片转存失败**：网络问题或原图失效时返回 \`None\`，不影响文章抓取流程
- **合规边界**：不建议默认转存第三方图片或将导出文件用于公开传播
- **临时文件清理**：导出文件在响应发送后由 \`BackgroundTask\` 自动删除

## 9. 相关文件

\`\`\`
apis/export.py                  # 导入/导出 API 路由
core/storage/minio_client.py    # MinIO 客户端
core/content_format.py          # 内容格式化（Markdown/PDF 相关）
core/rss.py                     # RSS 生成（含 OPML）
web_ui/src/views/TagList.tsx    # 标签列表（含导入/导出按钮）
web_ui/src/views/TagForm.tsx    # 标签编辑表单
\`\`\`
`,
  "docs/features/article-workbench": `# 文章工作台

文章列表页是 WeRSS 的核心工作台。无论你是做内容运营、标签整理，还是热点发现前的人工核查，大部分日常操作都会从这里开始。

## 1. 这个功能解决什么问题

它把“文章”作为统一入口，把系统里最常见的操作收在同一页：

- 搜索文章
- 按公众号筛选
- 查看正文和元数据
- 批量删除
- 批量提取标签
- AI 过滤低价值文章
- 导出或跳转到关联功能

如果没有这层工作台，后面的标签、热点、消息任务都只能在后台被动跑，人工无法快速干预。

## 2. 页面与接口边界

前端页面：

- [web_ui/src/views/ArticleListPage.tsx](../../web_ui/src/views/ArticleListPage.tsx)

核心接口：

- [apis/article.py](../../apis/article.py)

相关配套：

- 标签提取工具接口
- 订阅列表接口
- 导出弹窗组件

## 3. 工作台上的核心能力

### 搜索与筛选

当前页支持：

- 标题关键词搜索
- 按公众号筛选
- 分页
- 隐藏 AI 过滤后被判定为 \`hide\` 的文章

后端列表查询还支持更细的时间和标签参数，但当前主页面首先暴露的是高频筛选项。

### 文章详情查看

点击文章后可查看完整详情，包括：

- 标题
- 摘要
- 链接
- 封面图
- 正文
- 发布时间
- 所属公众号

如果正文为空，通常说明：

- 该文章只完成了基础入库
- 后台补全文任务还没跑完或失败

### 批量操作

当前页的批量操作是它最重要的价值之一。

包括：

- 批量删除
- 选中文章提取标签
- 当前页提取标签
- 全库重提标签
- 仅为缺标签文章补标签
- AI 过滤选中 / 全部文章
- 恢复被 AI 隐藏的文章

这里需要特别理解两件事：

### 标签提取

它不是单纯的“页面动作”，而是内容结构化的入口。标签提取质量直接影响：

- 标签管理页可用性
- 标签聚类结果
- 热点发现的辅助信号

### AI 过滤

AI 过滤的目标不是“删除文章”，而是给文章打一个保留判断，让列表页可以默认隐藏低价值内容。

这和物理删除不是一回事。

## 4. AI 过滤的真实行为

前端里这一块做得比一般列表页复杂。

在 [ArticleListPage.tsx](../../web_ui/src/views/ArticleListPage.tsx) 里，AI 过滤具备这些特征：

- 支持选中批次处理
- 支持全量处理
- 支持暂停、恢复和停止
- 会把任务状态写进 \`localStorage\`
- 刷新页面后可尝试恢复未完成任务

所以从产品上看，它不是一个“按钮一下就结束”的同步功能，而是一个带状态机的小任务系统。

## 5. 为什么说它是“工作台”

因为这里并不只是文章展示页，而是多个流程的交汇处：

- 订阅采集产出的文章先到这里
- 标签整理从这里开始
- RSS 最终基于这些文章输出
- 消息任务也依赖这里的数据
- 热点发现虽然是单独页面，但数据源仍然是这些文章

你可以把它理解为：

\`订阅 -> 文章工作台 -> 标签/热点/RSS/消息\`

## 6. 和其他功能的关系

### 与订阅管理

订阅管理决定“从哪里来”，文章工作台决定“怎么用”。

### 与标签系统

文章工作台负责批量把文章推进标签系统。

### 与热点发现

热点发现的输入是文章集合，工作台是人工校验这些文章质量的地方。

### 与导出和消息任务

它们都是文章的下游消费层。

## 7. 常见排查思路

### 搜不到文章

优先检查：

- 关键词是否命中标题
- 是否选了公众号筛选
- 是否被 AI 过滤后默认隐藏
- 后端分页参数是否正确

### 文章有标题但没正文

优先检查：

- 初次采集是否只拿到了元数据
- 补全文任务是否在跑
- 微信采集状态是否正常

### AI 过滤没生效

优先检查：

- 后端 AI 过滤接口是否成功返回
- 本地任务是否被暂停或中断
- 当前列表是否开启了“隐藏已过滤文章”

## 8. 谁应该先看这篇文档

- 需要日常处理文章的运营或管理者
- 要修改文章列表页的前端开发
- 要改文章查询、AI 过滤、标签提取接口的后端开发

如果你只看一篇功能文档就想快速理解 WeRSS，优先看这篇。
`,
  "docs/features/article-collection": `# 文章采集

## 1. 功能目标

在私有自用且有权访问相关内容的前提下，将公众号文章信息定时或手动写入 WeRSS 数据库，供后续的标签提取、RSS 输出、热点发现等功能使用。正文采集、历史批量抓取和内容补全应按需显式开启。

## 2. 适用场景

- 首次添加公众号后导入少量必要历史文章
- 定时增量采集已订阅公众号的新文章信息
- 文章内容被删除时重新抓取
- 采集失败时切换采集模式重试

## 3. 核心对象

| 对象 | 说明 |
|------|------|
| \`Article\` | 文章实体，\`articles\` 表 |
| \`mp\` | 公众号，\`mps\` 表 |
| \`subscription\` / \`Feed\` | 订阅（RSS 表现） |

## 4. 数据流 / 工作流程

\`\`\`
用户添加公众号订阅
        ↓
公众号记录写入 mps 表（通过 faker_id 关联微信账号）
        ↓
定时任务触发采集（jobs/mps.py）
        ↓
┌──────────────────────────────────────────┐
│  gather.content_mode 配置决定采集方式：    │
│                                          │
│  web 模式 → Playwright 浏览器驱动         │
│              driver/wxarticle.py         │
│              (WXArticleFetcher 类)       │
│                                          │
│  api 模式 → WxGather + WX_API             │
│              core/wx/wx.py                │
│              driver/wx.py                │
│                                          │
│  app 模式 → App 采集                      │
└──────────────────────────────────────────┘
        ↓
采集回调 UpdateArticle() 写入 articles 表
        ↓
入库后字段：id, mp_id, title, url, publish_time,
           pic_url, description, content, digest
\`\`\`

## 5. 主要能力

### 5.1 三种采集模式

**web 模式（默认）：**
- 使用 Playwright 浏览器模拟操作
- 位于 \`driver/wxarticle.py\` 的 \`WXArticleFetcher\` 类
- 支持提取阅读量、点赞量等统计数据
- 不应将该模式用于绕过登录、验证码、访问控制或平台技术保护措施

**api 模式：**
- 直接调用微信接口获取文章
- 位于 \`driver/wx.py\` 的 \`Wx\` 类和 \`core/wx/wx.py\`
- 依赖登录态（Session + Token）

**app 模式：**
- App 采集模式（未完全实现）

### 5.2 定时采集

- 调度器 \`TaskScheduler\`（\`core/task.py\`）按 cron 表达式执行
- \`jobs/mps.py\` 中的 \`start_job()\` 注册采集任务
- \`check_session_valid()\` 检查微信 Session 是否有效（登录状态 + cookie 剩余时间）

### 5.3 重新获取文章内容

- \`POST /api/v1/wx/articles/{article_id}/fetch_content\`
- 用于文章内容为空或被删除后重新抓取
- 支持 web / api 模式切换（配置项 \`gather.content_mode\`）

## 6. 关键配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| \`gather.content_mode\` | 采集模式：web / api / app | web |
| \`gather.content\` | 是否采集正文内容 | false |
| \`max_page\` | 每次采集的页数 | 1 |
| \`collect_start_date\` | 首次采集的起始日期 | 2025-12-01 |
| \`openai.api_key\` | AI API Key（用于标签提取） | - |

## 7. 关联页面与 API

- **Web UI：** \`/wechat/mp\`（公众号详情）、\`/subscriptions\`（订阅管理）
- **API：**
  - \`POST /api/v1/wx/mps\` - 添加公众号
  - \`GET /api/v1/wx/articles\` - 文章列表
  - \`POST /api/v1/wx/articles/{article_id}/fetch_content\` - 重新获取内容
  - \`DELETE /api/v1/wx/articles/clean\` - 清理孤立文章
  - \`DELETE /api/v1/wx/articles/clean_duplicate_articles\` - 清理重复文章

## 8. 常见问题 / 限制

- **Session 失效：** 微信登录态过期会导致 api 模式失败，需重新扫码登录
- **文章被删除：** 返回 \`DELETED\`，系统自动将文章状态标记为已删除
- **采集频率：** 每天最多采集 \`max_page\` 页（默认 1 页），可在配置中调整
- **内容为空：** 部分文章正文需要单独获取（\`fetch_content\`），可能因上游访问策略或页面结构变化失败
- **合规边界：** 不建议公开运营、批量抓取未授权内容、缓存全文或帮助他人分发第三方内容

## 9. 相关文件

\`\`\`
driver/wxarticle.py       # web 模式采集（Playwright）
driver/wx.py             # api 模式驱动
core/wx/wx.py           # WxGather 封装
core/wx/wx_api.py       # 微信 API 调用
jobs/mps.py             # 定时采集任务入口
jobs/article.py         # 文章更新回调
core/models/article.py  # Article 数据模型
core/models/feed.py     # Feed（订阅的 RSS 表现）
\`\`\`
`,
  "docs/dev/jobs-and-schedulers": `# 定时任务与调度器

本文档解释 WeRSS 后端里哪些能力依赖调度器、任务是怎么注册的、手动触发和自动触发有什么区别。

## 1. 先看结论

当前 \`jobs/\` 不是一个“可有可无的后台附属品”，而是系统主链路的一部分。至少这些能力依赖它：

- 公众号定时采集
- 消息任务定时推送
- 缺正文文章补全文
- 文章保留策略清理
- 热点发现

如果你只跑 API、不跑 job，系统仍然能打开，但很多后台能力不会自动工作。

## 2. 任务总入口

[jobs/__init__.py](../../jobs/__init__.py) 当前非常薄，真正的主入口在 \`jobs/mps.py\` 及各注册函数里。

你应该重点看：

- [jobs/mps.py](../../jobs/mps.py)
- [jobs/article_retention.py](../../jobs/article_retention.py)
- [jobs/hot_topics_job.py](../../jobs/hot_topics_job.py)
- [jobs/fetch_no_article.py](../../jobs/fetch_no_article.py)

## 3. 调度器的角色

项目里用的是统一调度器思路：

- 用 \`TaskScheduler\` 注册 cron 任务
- 不同 job 共用同一个调度器实例或调度机制
- 任务状态会在系统信息和设置页显示

从使用方式上看，它负责两类事：

### 3.1 真正的 cron 定时任务

例如：

- 每隔一段时间抓一次公众号
- 每天固定时间清理旧文章
- 每天固定时间跑热点发现

### 3.2 持续后台队列

例如缺正文文章补全文，虽然不一定表现为显式 cron，但属于后台持续处理逻辑。

## 4. 公众号采集任务

最核心的任务仍然在 [jobs/mps.py](../../jobs/mps.py)。

它做的事不止“采文章”，还包括：

- 检查微信 Session 是否有效
- 根据配置计算抓取页数
- 手动执行指定公众号更新
- 在消息任务里复用采集逻辑
- 维护任务队列状态

几个重要点：

- 会先做 \`check_session_valid()\`
- 采集起始时间会参考控制台配置 \`collect_start_date\`
- 有频率限制，不允许过于频繁地刷新同一公众号

这意味着如果用户说“页面点刷新没反应”，不能只看前端，优先查：

- 微信是否仍然登录
- 上次刷新时间是否太近
- \`server.enable_job\` 和启动参数是否正确

## 5. 缺正文补全任务

[jobs/fetch_no_article.py](../../jobs/fetch_no_article.py) 负责把已经有元数据、但正文缺失的文章继续补全。

这个任务的意义很大，因为系统里的文章并不是一次性都能拿到完整内容：

- 初次采集可能只有标题、摘要、封面
- 后续需要补抓正文

如果标签提取、热点发现效果变差，常见原因不是 AI 模型，而是正文补全率低。

## 6. 消息任务调度

消息任务是“用户配置任务，系统按 cron 跑”的典型例子。

相关代码主要在：

- [apis/message_task.py](../../apis/message_task.py)
- [jobs/taskmsg.py](../../jobs/taskmsg.py)
- [jobs/mps.py](../../jobs/mps.py)

运行形态：

1. 用户在页面里创建 \`MessageTask\`
2. 写入 \`message_tasks\` 表
3. 调度器读取 \`cron_exp\`
4. 到点后执行
5. 任务会抓对应公众号的文章并通过 Webhook 发送

需要注意的一点：

页面上的“应用”或“刷新调度”动作不是装饰，它的目的是让新的任务配置真正进入调度器。

## 7. 文章保留清理

[jobs/article_retention.py](../../jobs/article_retention.py) 只负责“注册”清理任务，真正删除逻辑在 \`core.article_retention\`。

它会读取这些配置：

- \`article.retention.enabled\`
- \`article.retention.cron\`
- \`article.retention.days\`
- \`article.retention.basis\`

所以这里要分清两层：

- 是否启用和何时执行：\`jobs/article_retention.py\`
- 实际删哪些文章：\`core/article_retention.py\`

## 8. 热点发现任务

热点发现既可以手动触发，也可以定时触发。

相关代码主要在：

- [apis/hot_topics.py](../../apis/hot_topics.py)
- [jobs/hot_topics_job.py](../../jobs/hot_topics_job.py)
- \`core.hot_topics.service\`

推荐理解方式：

- API 负责暴露重建入口和结果查询
- job 负责定时调度
- core 负责真正的聚类与持久化

定时注册与 **进程启动方式** 有关：需 \`main.py -job True\` 且 \`server.enable_job\` 为 true，热点任务才会随 \`start_all_task()\` 注册；详见 [docs/features/hot-topics.md](../features/hot-topics.md) 第 5.3 节。

## 9. 手动触发和自动触发的区别

这点经常被误解。

### 手动触发

通常来自页面按钮或 API：

- 刷新一个订阅
- 手动跑一次热点发现
- 手动执行一个消息任务

特点：

- 可立即反馈结果
- 更适合局部重试

### 自动触发

来自调度器：

- 定时采集
- 定时清理
- 定时热点分析

特点：

- 依赖启动参数和调度器状态
- 更适合持续运行

同一个能力可能同时支持两者，但它们的入口代码不一定相同。

## 10. 线上排查时先看什么

如果用户说“任务没跑”，建议按这个顺序查：

1. 后端是否用 \`-job True\` 启动
2. 对应功能的配置是否启用
3. 调度器状态页是否显示在运行
4. 任务是否注册成功
5. 微信 Session 是否失效
6. 消息任务 / 热点任务本身是否有报错

## 11. 新增一个 job 的建议方式

不要直接把逻辑全写进 \`jobs/\`。

更稳的方式是：

1. 先把业务逻辑写进 \`core/\`
2. \`jobs/\` 里只保留调度和触发包装
3. 需要时再在 \`apis/\` 里提供手动触发入口

这样同一套能力才能被：

- 页面按钮复用
- 定时任务复用
- 测试代码复用

## 12. 最容易踩的坑

- 只加了 API，没有注册后台 job。
- 只改了 cron 表达式，没有确认调度器已重载。
- 把业务逻辑写死在任务函数里，导致无法手动复用。
- 忘了任务需要微信登录状态。
- 用户以为“保存配置”就会自动生效，实际上还需要刷新调度器。
`,
  "docs/dev/frontend-structure": `# 前端结构

本文档面向要修改 \`web_ui/\` 的开发者。目标不是列目录，而是解释当前前端是怎么和后端、权限、页面状态配合工作的。

## 1. 前端的角色

当前前端是一个 React 18 + TypeScript + Vite 的管理台，不是“纯展示站”。它承担 4 类职责：

- 页面路由和登录拦截
- 调后端 API 并把返回结构转成页面状态
- 提供批量操作、弹窗、表格、筛选等后台交互
- 管理少量本地状态，比如主题、任务恢复状态

前端不应该自己重新实现后端业务规则。比如：

- AI 过滤结果怎么判断
- 热点怎么聚类
- 标签怎么抽取

这些都应来自后端。

## 2. 入口与路由

### \`src/main.tsx\`

这里只负责挂载应用。

### \`src/router/index.tsx\`

这是前端的真正入口。建议先读这个文件，因为它直接告诉你系统有哪些页面。

当前关键路由包括：

- \`/\`
- \`/articles\`
- \`/subscriptions\`
- \`/dashboard\`
- \`/settings\`
- \`/add-subscription\`
- \`/wechat/mp\`
- \`/configs\`
- \`/export/records\`
- \`/message-tasks\`
- \`/sys-info\`
- \`/tags\`
- \`/tag-clusters\`
- \`/hot-topics\`
- \`/api-keys\`

这里还有两层包装：

- \`ProtectedRoute\`
  没 token 就跳登录页。
- \`PermissionRoute\`
  预留权限检查入口。

如果你新增页面但访问不到，第一件事不是查组件，而是看是否已经加入 [web_ui/src/router/index.tsx](../../web_ui/src/router/index.tsx)。

## 3. 目录怎么读

\`web_ui/src/\` 可以按下面方式理解：

- \`views/\`
  页面级组件。通常一个文件对应一个页面。
- \`api/\`
  按领域封装后端接口。
- \`components/\`
  可复用组件和布局。
- \`router/\`
  路由定义。
- \`hooks/\`
  自定义 hooks。
- \`store/\`
  全局状态，例如主题。
- \`utils/\`
  日期、消息提示、常量等工具。
- \`types/\`
  接口类型定义。

页面逻辑应该尽量留在 \`views/\`，接口封装放到 \`api/\`，不要把 \`fetch\` 或 \`axios\` 直接散落在组件里。

## 4. 页面层：\`views/\`

当前可以分成 4 类页面：

### 4.1 内容主链路

- \`ArticleListPage.tsx\`
  文章工作台。
- \`SubscriptionManagement.tsx\`
  订阅管理。
- \`AddSubscription.tsx\`
  添加订阅。

### 4.2 分析与整理

- \`TagList.tsx\`
  标签管理。
- \`TagClusterList.tsx\`
  标签聚类列表。
- \`TagClusterDetail.tsx\`
  聚类详情。
- \`HotTopics.tsx\`
  热点发现。

### 4.3 系统运营

- \`MessageTaskList.tsx\`
- \`MessageTaskForm.tsx\`
- \`Settings.tsx\`
- \`ConfigList.tsx\`
- \`ConfigDetail.tsx\`
- \`SysInfo.tsx\`
- \`ApiKeyManagement.tsx\`

### 4.4 认证与用户

- \`Login.tsx\`
- \`ChangePassword.tsx\`
- \`EditUser.tsx\`

改页面前，先判断你是在改“列表页”、“表单页”还是“详情页”，不同页面的状态组织方式差别很大。

## 5. 组件层：\`components/\`

前端里最重要的不是原子组件，而是这些承载系统骨架的组件：

- \`components/Layout/BasicLayout\`
  登录后整个后台框架。
- \`components/ui/*\`
  基础 UI 组件。
- \`components/SystemResources\`
  系统资源卡片。
- 文章导出、消息提示、分页等业务组件

如果你改导航、右上角用户信息、菜单结构，一般落在 \`BasicLayout\`。

## 6. API 层：\`api/\`

\`api/\` 目录按后端领域分文件：

- \`article.ts\`
- \`subscription.ts\`
- \`tags.ts\`
- \`tagCluster.ts\`
- \`hotTopics.ts\`
- \`messageTask.ts\`
- \`configManagement.ts\`
- \`apiKey.ts\`
- \`sysInfo.ts\`

这里的规则应该是：

- 一个后端领域对应一个前端 API 文件
- 统一处理请求参数和返回值
- 页面不要重复拼 URL

如果一个页面同时发很多接口请求，优先检查能不能先补 API 封装，而不是在组件里临时写。

## 7. 状态管理怎么分层

当前前端有 3 类状态：

### 7.1 页面内局部状态

例如：

- 当前页码
- 搜索词
- 当前选中的文章
- 弹窗开关

这些大多用 \`useState\` 放在页面组件里。

### 7.2 可跨组件复用的全局状态

例如：

- 主题
- 可能的用户信息

这类状态放在 \`store/\`。

### 7.3 本地持久化状态

例如文章 AI 过滤任务会把一部分状态存到 \`localStorage\`，用于中断恢复。

不要把“临时页面状态”随意放全局。只有跨页面、跨组件或需要恢复的状态才值得提升。

## 8. 几个关键页面的实现特点

### 文章列表页

[web_ui/src/views/ArticleListPage.tsx](../../web_ui/src/views/ArticleListPage.tsx) 是最复杂的页面之一，主要特征：

- 顶部筛选
- 公众号下拉
- 批量操作
- AI 过滤任务恢复
- 标签提取任务
- 导出
- 文章详情抽屉 / 弹窗

这里适合做增量改造，不适合一次性大重构。

### 订阅管理页

[web_ui/src/views/SubscriptionManagement.tsx](../../web_ui/src/views/SubscriptionManagement.tsx) 是“列表 + 详情”双栏页：

- 左侧列表
- 右侧详情
- 支持直接刷新采集
- 支持编辑 / 删除

如果你要加新字段，一般要同时改列表区和详情区。

### 设置页

[web_ui/src/views/Settings.tsx](../../web_ui/src/views/Settings.tsx) 不是简单偏好页，它混合了：

- 本地 UI 设置
- 配置管理 API
- 系统调度器信息
- 文章清理策略
- Prompt 模板

所以改设置页前，要先分清楚：

- 这是纯前端设置
- 还是会写入后端配置
- 还是只读系统状态

## 9. 新增一个页面的正确方式

建议按这个顺序：

1. 在 \`views/\` 新建页面组件
2. 在 \`api/\` 补接口封装
3. 在 \`router/index.tsx\` 注册路由
4. 需要时把菜单入口加到布局组件
5. 补类型定义和空态 / 错态

不要先把页面写完再回头补 API；那样通常会把返回结构假设错。

## 10. 最容易踩的坑

- 改了页面但没同步 \`router\`。
- 直接在页面里手写请求逻辑，后面无法复用。
- 把权限问题当成前端问题，其实是后端没拦。
- 没看后端实际返回结构，就按旧接口印象写页面。
- 列表页只改展示，不改分页和筛选逻辑。

## 11. 建议阅读顺序

第一次进前端建议按这个顺序读：

1. [web_ui/src/router/index.tsx](../../web_ui/src/router/index.tsx)
2. [web_ui/src/components/Layout/BasicLayout.tsx](../../web_ui/src/components/Layout/BasicLayout.tsx)
3. [web_ui/src/views/ArticleListPage.tsx](../../web_ui/src/views/ArticleListPage.tsx)
4. [web_ui/src/views/SubscriptionManagement.tsx](../../web_ui/src/views/SubscriptionManagement.tsx)
5. [web_ui/src/views/Settings.tsx](../../web_ui/src/views/Settings.tsx)

这样最容易抓住项目的前端主干。
`,
  "docs/dev/extension-guide": `# 扩展指南

本文档给要继续扩展 WeRSS 的开发者用。重点不是“能不能加功能”，而是“按当前项目边界，应该从哪里下手才不会把系统越改越乱”。

## 1. 扩展前先分类型

先判断你要加的是哪一类能力：

- 新 API
- 新页面
- 新后台任务
- 新分析模块
- 新配置项
- 新导入导出格式

不同类型落点不同，不要默认一上来就在 \`apis/\` 里开新文件。

## 2. 新增一个 API

### 什么时候应该加 API

只有当能力需要被前端或外部程序主动调用时，才值得加 API。

例如：

- 新增一个“手动重建”入口
- 新增一个列表查询接口
- 新增一个导出接口

### 基本步骤

1. 确认归属领域
2. 在 \`apis/\` 下补或新建文件
3. 业务逻辑尽量写到 \`core/\`
4. 在 \`web.py\` 注册 router
5. 前端在 \`web_ui/src/api/\` 补调用封装

### 例子

- 文章相关：\`apis/article.py\`
- 标签相关：\`apis/tags.py\`
- 消息任务：\`apis/message_task.py\`
- 热点发现：\`apis/hot_topics.py\`

如果只是现有领域里的一个补充动作，优先放进原文件，不要轻易拆新模块。

## 3. 新增一个页面

页面扩展建议按下面顺序：

1. 明确页面属于哪个导航组
2. 新建 \`views/*.tsx\`
3. 在 \`router/index.tsx\` 注册路由
4. 在 \`api/\` 层补接口封装
5. 需要时把入口接到 \`BasicLayout\`

新增页面时最容易犯的错是：

- 先写页面，后补接口
- 结果页面结构是按想象写的，和后端返回对不上

更稳的做法是：

- 先确认 API 数据形状
- 再写页面

## 4. 新增一个分析能力

像标签聚类、热点发现这类能力，不应该直接写成“某个接口里的一段大函数”。

推荐边界：

- \`core/\`
  放分析逻辑本体
- \`jobs/\`
  放定时运行
- \`apis/\`
  放手动触发和结果查询
- \`web_ui/\`
  放展示页

这样同一个能力才能支持：

- 手动跑
- 自动跑
- 结果回溯

## 5. 新增一个定时任务

建议流程：

1. 先把业务逻辑写到 \`core/\`
2. 在 \`jobs/\` 注册 cron 或后台任务
3. 在系统信息 / 设置页补状态展示
4. 如果用户需要手动触发，再补 API

不要把完整业务都堆到 \`jobs/*.py\` 里。\`jobs/\` 更适合“触发器”，不是“业务主仓库”。

## 6. 新增一个配置项

改配置必须同步考虑 4 个地方：

1. \`config.example.yaml\`
2. \`.env.example\`
3. \`core/config.py\`
4. 如果需要控制台编辑，再加到 \`apis/config_management.py\` 或设置页

还要判断这个配置是：

- 启动时读取一次
- 还是运行时可热更新

如果涉及调度器，通常保存配置后还要刷新调度器状态。

## 7. 新增一个导入导出能力

这类能力当前主要在 \`apis/export.py\`。

建议方式：

- 先定义导出的数据形状
- 确认权限与下载方式
- 再决定是 CSV、OPML、JSON 还是别的格式

新增导出时，不要只考虑“导出”，还要考虑：

- 是否有对应导入
- 字段能否回灌
- 临时文件是否要自动清理

## 8. 什么时候该复用，什么时候该新建

### 优先复用

- 已有领域 API 文件
- 已有数据模型
- 已有页面布局
- 已有任务调度器
- 已有 \`success_response\` / \`error_response\`

### 可以新建

- 明显是新领域
- 旧文件已经过大且边界清楚
- 新能力有稳定独立的数据模型和页面

## 9. 一个完整功能怎么落地

以“新增一个内容分析页”为例，完整落地通常是：

1. \`core/\` 写分析逻辑
2. \`core/models/\` 补结果实体
3. \`apis/\` 提供结果列表和重建接口
4. \`jobs/\` 注册定时任务
5. \`web_ui/src/api/\` 补前端接口
6. \`web_ui/src/views/\` 补页面
7. \`router/index.tsx\` 注册路由
8. \`docs/\` 补功能文档和 UI 文档

如果缺其中任意一层，后面通常都会返工。

## 10. 最容易把系统搞乱的几种方式

- 直接在 API 里写完整业务。
- 页面里直接写请求逻辑和数据清洗。
- 任务逻辑与手动逻辑各写一份。
- 新增配置但不写默认值和优先级说明。
- 新增字段但不补文档和导出层。

## 11. 建议的扩展顺序

如果你在做一个新功能，建议按下面顺序推进：

1. 先确认数据模型
2. 再写业务逻辑
3. 再暴露 API
4. 再接前端
5. 最后补文档和运维说明

这个顺序虽然不花哨，但返工最少。
`,
  "docs/dev/development-log": `# 更新日志

本文档记录 WeRSS 的版本更新。\`README\` 只保留最近几次更新摘要，完整记录见本页。

## v1.1.5（当前开发线）

**科技热点与主链路**

- 热点发现接入主链路：API、任务与结果模型、前端热点页，支持时间窗口分析与手动重跑
- 从关键词堆叠升级为围绕「事件型主题」的聚合

**文档与交付**

- 独立文档站与 GitHub Pages 发布；文档按层拆分，\`README\` 统一指向线上站点
- 本页与版本记录从 \`README\` 拆出，便于持续维护

**运维与存储**

- 支持文章保留策略（启用、天数、判断基准可配）
- MinIO 图片转存可明确开关，便于控成本、控资源

**标签与后台**

- Doubao embedding 维度等配置补强；聚类重建与后台体验优化
- API Key 与公众号信息更新等管理能力补齐

## v1.1.4（2026-04-21）

**内容分析增强**

- 新增 AI 文章过滤能力，可对文章进行保留、隐藏、待确认等分类处理
- 新增标签聚合与语义聚类能力，支持更清晰地组织和查看标签关系
- 新增微信公众号文章热度指标抓取，支持采集阅读、点赞、在看、评论、转发等数量信息

**体验优化**

- 优化文章列表与设置页交互，精简无效提示文案和重复操作入口
- 调整部分管理页面在浅色模式下的卡片背景，统一为更清晰的白色面板

## v1.1.3（2026-04-17）

**功能优化**

- 修复并优化标签聚类算法，提升标签标准化效果与聚类详情页可用性
- 修复 MCP 支持相关问题，提升整体接入稳定性
- 完成前端色彩体系向绿色系迁移，统一整体视觉风格

## v1.1.2（2026-04-16）

**标签体系增强**

- 新增标签语义聚类：基于标签 profile、文章共现与 embedding 生成标签主题簇
- 新增相似标签视图与聚类详情页
- 支持聚类 JSON 导出与合并建议展示
- 支持 BigModel / Doubao embedding provider 配置

**部署与维护**

- 修复 \`DB\` 环境变量覆盖问题，避免 compose 锁死到错误数据库
- 新增标签聚类部署与验证清单

## v1.1.1（2026-04-15）

**功能优化**

- 应用设置页面新增"定时抓取任务"卡片，可查看调度器运行状态、任务数量及下次执行时间
- 修复水印功能占位符问题，现已显示实际版权文本水印

**代码质量**

- 使用 asyncio.to_thread 优化 Playwright 同步 API 在异步处理器中的调用

## v1.1.0（2026-03-21）

**采集稳定性**

- 二维码登录存活时间延长并支持自动刷新，但是由于微信限制每天还是要重新扫码登录。
- 爬取间隔 \`SPAN_INTERVAL\` 默认从 10 调高到 30，降低被微信流控/封禁的风险
- \`MAX_PAGE\` 默认从 5 调低到 3，减少单次连续请求时长
- User-Agent 去除 \`WeRss\` 标识，改为模拟真实 Chrome 浏览器

**部署架构重构**

- Compose 文件从 4 个叠加简化为 2 个：\`docker-compose.yml\`（完整栈）和 \`docker-compose.app-only.yml\`（仅应用）
- 新用户 \`cp .env.example .env && docker compose up -d\` 一键启动
- 子项目模式通过环境变量 \`WERSS_EXTERNAL_NETWORK\` / \`WERSS_TRAEFIK_ENABLE\` 配置，无需叠加额外 compose 文件
- \`.env.example\` 重写为 4 个清晰区块，移除 WeRSS 不使用的变量
- 全局匿名化，移除所有私有路径和域名引用
- 脚本移除 monorepo 假设（不再加载上级目录 \`.env\`）
- 文档合并为统一的 \`docs/DEPLOYMENT.md\`

## v1.0.0

初始发布版本。

## 维护约定

后续每次版本更新建议至少补充：版本号、主要变化、指向的功能文档或 UI 文档链接（如有）。
`,
  "docs/dev/data-model-overview": `# 数据模型概述

本文档解释 WeRSS 当前最重要的数据实体、它们之间的关系，以及改字段时应该先想到什么。

## 1. 先抓主干

如果只记住一条主线，应该是：

\`Feed(公众号订阅) -> Article(文章) -> Tag / ArticleTag(标签) -> TagCluster(标签聚类) -> HotTopic(热点主题)\`

外围再挂：

- \`MessageTask\`
- \`ApiKey\`
- \`ConfigManagement\`
- \`User\`

## 2. 公众号与订阅

### Feed

[core/models/feed.py](../../core/models/feed.py)

\`Feed\` 是当前系统里最核心的“公众号入口实体”，字段包括：

- \`id\`
  主键，同时也是很多地方使用的 \`mp_id\`
- \`mp_name\`
  公众号名称
- \`mp_cover\`
  头像或封面
- \`mp_intro\`
  简介
- \`status\`
  启用状态
- \`sync_time\`
  同步时间
- \`update_time\`
  上次更新时间
- \`faker_id\`
  微信侧抓取和识别时使用的关联标识

需要注意：

- 前端通常把它叫“订阅”
- 文章表里 \`Article.mp_id\` 实际就是对 \`Feed.id\` 的引用语义

## 3. 文章

### Article

[core/models/article.py](../../core/models/article.py)

文章是系统里最重的实体，至少承担这些职责：

- RSS 输出的原始内容
- 标签提取的输入
- 热点发现的输入
- 消息任务推送的内容源

关键字段通常包括：

- \`id\`
  文章唯一标识
- \`mp_id\`
  所属公众号
- \`title\`
  标题
- \`url\`
  原文链接
- \`description\`
  摘要
- \`content\`
  正文
- \`pic_url\`
  封面图
- \`publish_time\`
  发布时间
- \`created_at\`
  入库时间
- \`status\`
  删除或禁用状态

几个关键点：

- \`publish_time\` 和 \`created_at\` 不是一回事
- 文章保留策略可按两者之一作为基准
- 有些文章初次入库时正文可能为空，后续由后台补全

## 4. 标签

### Tags

[core/models/tags.py](../../core/models/tags.py)

标签是文章的二次结构化层。关键字段包括：

- \`id\`
- \`name\`
- \`cover\`
- \`intro\`
- \`status\`
- \`mps_id\`
- \`is_custom\`
- \`created_at\`
- \`updated_at\`

这里有两个要点：

### \`is_custom\`

表示是否为用户手动定义标签。它很重要，因为：

- 手动标签常常代表业务偏好
- AI 提取时可能会优先参考

### \`mps_id\`

是标签和公众号维度关联的扩展字段，别把它理解成简单备注。

## 5. 文章与标签关系

### ArticleTag

[core/models/article_tags.py](../../core/models/article_tags.py)

这是典型的中间表，用来表达：

- 一篇文章可以有多个标签
- 一个标签可以关联多篇文章

很多页面上的“文章数量”“标签数量”都不是直接表字段，而是由这张表统计出来的。

如果你改标签删除逻辑，必须考虑 \`ArticleTag\` 是否需要联动清理。

## 6. 标签聚类相关实体

标签聚类不是只有一张表，当前至少包括：

- \`TagCluster\`
- \`TagClusterMember\`
- \`TagClusterAlias\`
- \`TagEmbedding\`
- \`TagSimilarity\`
- \`TagProfile\`

### TagCluster

[core/models/tag_clusters.py](../../core/models/tag_clusters.py)

关键字段：

- \`id\`
- \`name\`
- \`description\`
- \`centroid_tag_id\`
- \`size\`
- \`cluster_version\`

它代表的是“一个聚类结果快照中的一个簇”，不是永久不变的业务实体。

也就是说：

- 重建聚类后，簇可能重新分配
- \`cluster_version\` 很重要，不能忽略

## 7. 热点发现相关实体

热点发现是另一条分析链，不和标签聚类混为一谈。

### HotTopicRun

[core/models/hot_topic_runs.py](../../core/models/hot_topic_runs.py)

表示一次热点分析运行。关键字段：

- \`id\`
- \`window_days\`
- \`article_count\`
- \`status\`
- \`model_name\`
- \`prompt_version\`
- \`raw_request\`
- \`raw_response\`
- \`error_message\`

### HotTopic

[core/models/hot_topics.py](../../core/models/hot_topics.py)

表示某次运行产出的一个热点主题。关键字段：

- \`id\`
- \`run_id\`
- \`topic_name\`
- \`summary\`
- \`signals_json\`
- \`article_count\`
- \`sort_order\`

### HotTopicArticle

[core/models/hot_topic_articles.py](../../core/models/hot_topic_articles.py)

表示热点主题和文章之间的关联。

这套结构意味着：

- 热点不是直接挂在文章上的静态字段
- 它是某次分析运行的结果集
- 你可以追溯某次运行用了哪些文章、产出了哪些主题

## 8. 消息任务

### MessageTask

[core/models/message_task.py](../../core/models/message_task.py)

关键字段：

- \`id\`
- \`name\`
- \`message_type\`
- \`message_template\`
- \`web_hook_url\`
- \`mps_id\`
- \`cron_exp\`
- \`status\`

这里最容易误解的是 \`mps_id\`：

- 它不是单个公众号 ID
- 当前实现里常常是一个集合或 JSON 字符串语义

所以改消息任务时不要直接把它当普通外键。

## 9. API Key 与调用日志

### ApiKey

[core/models/api_key.py](../../core/models/api_key.py)

主要用于：

- 外部程序访问 API
- 区分用户密钥
- 控制启用状态和权限串

### ApiKeyLog

记录 API Key 的使用情况：

- 调了哪个 endpoint
- 用了什么 method
- 返回状态码
- 调用时间

如果你要做审计或限流，这是第一批会用到的数据。

## 10. 配置实体

### ConfigManagement

[core/models/config_management.py](../../core/models/config_management.py)

它表示“控制台里写入数据库的配置覆盖项”，不是全量配置源。

系统配置真实来源有多层：

- \`config.yaml\`
- \`.env\`
- 数据库覆盖
- 运行时环境变量

所以不要把它误当成唯一配置表。

## 11. 用户实体

### User

[core/models/user.py](../../core/models/user.py)

和本文档最相关的点只有两个：

- 登录认证依赖它
- API Key 归属依赖它

另外当前产品要求里，\`username\` 应视为稳定标识，展示名优先用 \`nickname\`。

## 12. 改模型时的检查清单

任何字段改动，都至少检查这几层：

1. 模型定义
2. API 返回
3. 前端展示
4. 导入导出
5. 定时任务是否依赖
6. 历史数据兼容性

最容易出问题的不是“加字段”，而是：

- 改字段语义
- 改字段类型
- 改默认值

因为这些会同时影响筛选、排序、导出和历史数据。
`,
  "docs/dev/configuration-and-precedence": `# 配置与优先级

## 1. 文档目的

本文档面向部署工程师和开发者，解释 WeRSS 的配置来源、加载顺序和优先级规则。帮助解决"改了配置不生效""环境变量和数据库哪个优先"等问题。

## 2. 配置来源

WeRSS 配置有四个来源，按优先级从低到高：

| 来源 | 说明 |
|---|---|
| \`config.example.yaml\` | 默认配置示例文件，不参与实际加载 |
| \`config.yaml\` | 用户配置文件，支持 \`\${VAR:-default}\` 环境变量插值 |
| \`.env\` | 开发环境环境变量文件（仅开发环境加载） |
| 数据库 \`config_management\` 表 | Web UI 控制台写入的配置 |
| 环境变量 | 运行时环境变量，直接覆盖 yaml 值 |

## 3. 配置加载流程

\`core/config.py\` 的 \`Config.get()\` 方法实现三层解析：

\`\`\`
1. 解析 config.yaml（支持 \${VAR:-default} 插值）
2. 解析环境变量覆盖（已展开的值优先于 yaml 中未展开的）
3. 解析数据库覆盖（WERSS_ENV_OVERRIDES_DB 控制优先级）
\`\`\`

### 3.1 yaml + 环境变量插值

\`config.yaml\` 中的 \`\${VAR:-default}\` 语法：
- \`VAR\`：环境变量名
- \`default\`：如果环境变量未设置，使用此默认值

**示例**：
\`\`\`yaml
db: \${DB:-sqlite:///data/db.db}
\`\`\`
如果环境变量 \`DB\` 未设置，使用 \`sqlite:///data/db.db\`；否则使用 \`DB\` 的值。

### 3.2 数据库覆盖层

Web UI 的"系统设置"页面保存配置时，写入 \`config_management\` 表。

数据库覆盖的优先级由 \`WERSS_ENV_OVERRIDES_DB\` 环境变量控制：

| \`WERSS_ENV_OVERRIDES_DB\` | 行为 |
|---|---|
| 未设置 / \`false\`（默认） | \`config_management\` 表优先。只要表中有该键，返回表中的值；仅当表中无该键时回退到 yaml/环境变量。 |
| \`true\` | yaml + 环境变量优先。仅当 yaml/环境变量中该键未设置或为空字符串时，才回退到 \`config_management\` 表。 |

**\`WERSS_ENV_OVERRIDES_DB\` 的典型场景**：
- 共享部署（多实例共用数据库）：设为 \`true\`，各实例用环境变量覆盖自己想覆盖的键
- 单实例部署：默认行为即可，控制台保存的配置直接生效

### 3.3 特殊键 \`db\`

数据库连接串 \`db\` 永远不走 \`config_management\` 表覆盖，只从 \`config.yaml\` + 环境变量读取。

## 4. 配置优先级总结

以 \`minio.store_article_images\` 为例，假设在 Web UI 中保存为 \`false\`：

| 环境变量 \`MINIO_STORE_ARTICLE_IMAGES\` | \`WERSS_ENV_OVERRIDES_DB\` | 实际值 |
|---|---|---|
| 未设置 | \`false\`（默认） | \`false\`（来自数据库） |
| \`true\` | \`false\`（默认） | \`true\`（环境变量覆盖 yaml，但数据库优先） |
| \`true\` | \`true\` | \`true\`（环境变量 > yaml > 数据库，且非空） |
| 未设置 | \`true\` | 数据库回退值（如果有） |

**简化原则**：
- \`WERSS_ENV_OVERRIDES_DB=false\`（默认）：数据库 > yaml > 环境变量
- \`WERSS_ENV_OVERRIDES_DB=true\`：环境变量/yaml > 数据库（仅空时回退）

## 5. 关键配置项

### 5.1 数据库连接

\`\`\`yaml
db: \${DB:-sqlite:///data/db.db}
\`\`\`

支持：
- SQLite：\`sqlite:///data/db.db\`
- MySQL：\`mysql+pymysql://user:password@host:3306/werss?charset=utf8mb4\`
- PostgreSQL：\`postgresql://user:password@host:5432/werss\`

**注意**：连接串格式必须正确，MySQL 用 \`mysql+pymysql://\`，PostgreSQL 用 \`postgresql://\`。

### 5.2 定时任务开关

\`\`\`yaml
server:
  enable_job: \${ENABLE_JOB:-True}
\`\`\`

与启动参数 \`-job True\` 共同决定是否启动 APScheduler 调度器。两者都为真才启动。

### 5.3 文章清理

\`\`\`yaml
article:
  true_delete: \${ARTICLE.TRUE_DELETE:-True}  # true=物理删除，false=逻辑删除（标记 status）
  retention:
    enabled: \${ARTICLE_RETENTION_ENABLED:-false}
    days: \${ARTICLE_RETENTION_DAYS:-7}
    basis: \${ARTICLE_RETENTION_BASIS:-created_at}  # created_at 或 publish_time
    cron: \${ARTICLE_RETENTION_CRON:-0 4 * * *}  # 默认每天 4:00
\`\`\`

### 5.4 MinIO 对象存储

\`\`\`yaml
minio:
  enabled: \${MINIO_ENABLED:-False}
  store_article_images: \${MINIO_STORE_ARTICLE_IMAGES:-False}
  endpoint: \${MINIO_ENDPOINT:-localhost:9000}
  access_key: \${MINIO_ACCESS_KEY:-minioadmin}
  secret_key: \${MINIO_SECRET_KEY:-minioadmin}
  bucket: \${MINIO_BUCKET:-articles}
  secure: \${MINIO_SECURE:-False}
  public_url: \${MINIO_PUBLIC_URL:-http://localhost:9000}
\`\`\`

### 5.5 RSS

\`\`\`yaml
rss:
  base_url: \${RSS_BASE_URL:-}  # RSS 域名，填写真实域名后 RSS 链接才正确
  local: \${RSS_LOCAL:-False}  # True=本地代理链接，False=直接输出微信原文链接
  full_context: \${RSS_FULL_CONTEXT:-False}  # True=输出全文，False=仅输出摘要
  add_cover: \${RSS_ADD_COVER:-False}  # True=添加封面图片 enclosure
  cdata: \${RSS_CDATA:-False}  # True=内容用 CDATA 包裹
  page_size: \${RSS_PAGE_SIZE:-10}  # 每页文章数量
\`\`\`

### 5.6 标签提取

\`\`\`yaml
article_tag:
  auto_extract: \${ARTICLE_TAG_AUTO_EXTRACT:-False}
  max_tags: \${ARTICLE_TAG_MAX_TAGS:-5}

openai:
  api_key: \${OPENAI_API_KEY:-}
  base_url: \${OPENAI_BASE_URL:-https://api.openai.com/v1}
  model: \${OPENAI_MODEL:-gpt-4o}
\`\`\`

### 5.7 采集配置

\`\`\`yaml
gather:
  content: \${GATHER.CONTENT:-False}  # 是否采集正文
  model: \${GATHER.MODEL:-app}  # app/api/web
  browser_type: \${BROWSER_TYPE:-firefox}
  content_auto_check: \${GATHER.CONTENT_AUTO_CHECK:-False}
\`\`\`

## 6. .env 文件（仅开发环境）

开发环境下，\`core/config.py\` 会在初始化前尝试加载项目根目录的 \`.env\` 文件：

\`\`\`python
# 仅在非 Docker 环境中加载
if not is_docker:
    load_dotenv('.env', override=False)
\`\`\`

Docker 部署时不加载 \`.env\`，环境变量由 \`docker-compose.yml\` 注入。

## 7. 常见问题

### 7.1 改了 .env 配置文件不生效

检查：
1. 是否在 Docker 环境（Docker 环境不加载 \`.env\`）
2. 是否设置了 \`WERSS_ENV_OVERRIDES_DB=true\`（此时 yaml/环境变量优先于数据库）
3. \`config_management\` 表中是否存在该键（数据库值可能覆盖了环境变量）

### 7.2 Web UI 保存的配置不生效

原因：\`WERSS_ENV_OVERRIDES_DB=true\` 时，环境变量和 yaml 优先于数据库。

解决：在 \`.env\` 或 \`config.yaml\` 中明确设置该配置项，或将 \`WERSS_ENV_OVERRIDES_DB\` 设为 \`false\`。

### 7.3 文章清理不执行

需要同时满足：
- \`article.retention.enabled=true\` 或环境变量 \`ARTICLE_RETENTION_ENABLED=true\`
- \`server.enable_job=true\`（\`ENABLE_JOB=True\`）
- 进程启动时带 \`-job True\` 参数

### 7.4 MinIO 图片转存不生效

检查：
1. \`minio.enabled=true\`
2. MinIO 服务可访问（endpoint 正确，防火墙开放 9000 端口）
3. \`minio.store_article_images=true\`（或在 Web UI 设置中开启）
4. 存储桶已创建且 access_key/secret_key 正确

## 8. 相关文件

- \`core/config.py\` — 配置加载器，\`cfg.get()\` 实现
- \`core/config_overrides.py\` — 数据库覆盖层，\`WERSS_ENV_OVERRIDES_DB\` 逻辑
- \`config.example.yaml\` — 配置示例
- \`docs/DEPLOYMENT.md\` — 部署文档（包含 docker-compose 环境变量说明）
`,
  "docs/dev/backend-structure": `# 后端结构

本文档面向要改后端 API、任务逻辑或数据流的开发者。目标是让你在读代码前先知道每一层该负责什么，避免把业务塞错地方。

## 1. 先建立整体心智

当前后端可以按 5 层来理解：

1. \`main.py\`
   进程入口，负责解析启动参数。
2. \`web.py\`
   FastAPI 应用装配，注册路由、中间件、静态资源和 SPA 兜底。
3. \`apis/\`
   HTTP 路由层，负责参数接收、权限校验、返回格式。
4. \`core/\`
   真正的业务逻辑、模型、配置、微信采集、对象存储、AI 处理。
5. \`jobs/\`
   定时任务与异步触发层，负责采集、补全文、消息推送、文章清理、热点发现。

如果一个改动同时涉及“接口行为”和“实际业务”，通常要同时看 \`apis/\` 和 \`core/\`，不要只改一层。

## 2. 入口文件

### \`main.py\`

\`main.py\` 不是业务入口，而是进程入口。它主要处理：

- 初始化数据库或系统资源
- 读取 \`-job\`、\`-init\` 之类的启动参数
- 启动 \`uvicorn\`

你通常不会在这里加业务代码。适合放在这里的只有：

- 启动参数
- 进程级初始化
- 启动模式切换

### \`web.py\`

\`web.py\` 是 HTTP 应用入口，职责非常集中：

- 创建 FastAPI 实例
- 注册所有 \`apis/*\` 路由
- 配置 OpenAPI 文档
- 配置认证、中间件、CORS
- 挂载 \`/assets\`、\`/static\`、\`/files\`
- 提供前端 SPA 兜底

如果你新增了一个 API 文件，但前端请求 404，第一件事就是检查 \`web.py\` 里是否 \`include_router\`。

## 3. 路由层：\`apis/\`

\`apis/\` 的职责是“把 HTTP 请求翻译成系统动作”，它不应该承载复杂业务。

当前主要模块如下：

- \`auth.py\`
  登录、令牌、用户身份相关。
- \`article.py\`
  文章列表、筛选、详情、删除、AI 过滤等。
- \`mps.py\`
  公众号搜索、订阅管理、手动刷新文章。
- \`rss.py\`
  RSS / Atom / JSON Feed 输出。
- \`tags.py\`
  标签 CRUD、测试提取、状态管理。
- \`tag_clusters.py\`
  标签聚类列表、详情、重建、导出、可视化概览。
- \`hot_topics.py\`
  科技热点列表、重建、运行记录查询。
- \`message_task.py\`
  消息任务 CRUD、手动执行。
- \`config_management.py\`
  控制台配置项的读写。
- \`sys_info.py\`
  系统资源、版本、调度器和微信登录状态。
- \`api_key.py\`
  API Key 管理和调用日志。
- \`export.py\`
  公众号和标签导入导出。

路由层应当做这些事：

- 解析 Query / Body / Path
- 认证和权限校验
- 调用 \`core/\` 或 \`jobs/\` 里的能力
- 统一返回 \`success_response\` / \`error_response\`

不应该做这些事：

- 写大段业务规则
- 直接堆复杂 SQL
- 在请求函数里维护长期状态

## 4. 业务层：\`core/\`

\`core/\` 是项目最重的一层，读代码时可以再拆成几类：

### 4.1 配置与基础设施

- \`core/config.py\`
  配置读取与优先级整合。
- \`core/config_overrides.py\`
  配置覆盖缓存。
- \`core/db.py\` / \`core/database.py\`
  数据库 session 与基础访问。

### 4.2 数据模型

- \`core/models/*\`
  SQLAlchemy 模型定义。

常用实体包括：

- \`Feed\`
  公众号订阅实体。
- \`Article\`
  文章实体。
- \`Tags\`
  标签实体。
- \`ArticleTag\`
  文章与标签关联。
- \`MessageTask\`
  消息任务。
- \`ApiKey\` / \`ApiKeyLog\`
  API 密钥和调用日志。
- \`TagCluster*\`
  标签聚类相关表。
- \`HotTopic*\`
  热点发现相关表。

### 4.3 微信采集与解析

- \`core/wx/\`
  微信侧查询和抓取逻辑。
- \`driver/\`
  Playwright / 浏览器驱动、登录状态、token。

### 4.4 AI 与内容处理

- \`core/tag_extractor.py\`
  标签提取。
- \`core/tag_cluster.py\`
  标签聚类重建。
- \`core/article_filter.py\`
  AI 文章过滤。
- \`core/hot_topics/\`
  热点发现。

### 4.5 资源与存储

- \`core/res.py\`
  头像与资源保存。
- \`core/minio_*\`
  对象存储能力。
- \`core/article_retention.py\`
  文章清理逻辑。

## 5. 任务层：\`jobs/\`

\`jobs/\` 负责“时间驱动”或“后台驱动”的动作，不是给前端直接调用的业务层。

当前关键任务包括：

- \`jobs/mps.py\`
  公众号文章采集、消息任务执行、调度器核心入口。
- \`jobs/fetch_no_article.py\`
  为缺正文的文章补全文。
- \`jobs/article_retention.py\`
  注册文章清理任务。
- \`jobs/hot_topics_job.py\`
  注册热点发现任务。
- \`jobs/taskmsg.py\`
  消息任务调度支持。
- \`jobs/webhook.py\`
  Webhook 推送。

如果某个能力既支持“页面按钮手动触发”又支持“定时触发”，通常页面会走 API，而 API 内部再调用 \`jobs/\` 或和 \`jobs/\` 共用同一套底层逻辑。

## 6. 一条真实链路怎么走

以“订阅管理页手动刷新一个公众号”为例：

1. 前端调用 \`GET /api/v1/wx/mps/update/{mp_id}\`
2. 路由在 [apis/mps.py](../../apis/mps.py) 做频率限制和基础校验
3. 内部通过 \`WxGather\` 拉取文章
4. 回调 \`jobs/article.py\` 里的 \`UpdateArticle\`
5. 文章写入 \`Article\` 表
6. 页面轮询或刷新后重新读订阅统计

再比如“热点发现”：

1. 前端访问 \`/hot-topics\`
2. 页面读取 \`GET /api/v1/wx/hot-topics/recent\`（无需登录）
3. 路由从 \`HotTopicRun\`、\`HotTopic\`、\`HotTopicArticle\` 取最新结果
4. 如果用户手动重建，则调用 \`POST /api/v1/wx/hot-topics/rebuild\`（需登录）
5. 后端进入 \`core.hot_topics.service.run_discovery\`

## 7. 常见改动应该落在哪

### 新增一个接口

落点：

- 新建或修改 \`apis/*.py\`
- 在 \`web.py\` 注册 router
- 需要时补 \`core/\` 业务逻辑

### 新增一个后台分析能力

落点：

- 业务逻辑写在 \`core/\`
- 如果需要定时执行，再补 \`jobs/\`
- 只在 \`apis/\` 暴露控制接口和结果查询

### 改配置项

落点：

- \`core/config.py\`
- \`config.example.yaml\`
- \`.env.example\`
- 若支持控制台覆盖，再看 \`apis/config_management.py\`

### 改页面展示字段

不要只改前端。

先确认：

- API 是否已经返回字段
- 模型是否有字段
- 后端是否正确序列化

## 8. 最容易踩的坑

- 把业务写在路由层，后面无法复用。
- 改了模型字段，没有同步任务层和导出层。
- 只改 \`web_ui\`，忘了后端接口返回结构不匹配。
- 只改 API，不看 \`jobs/\` 是否还有一份同逻辑。
- 修改配置项但不检查环境变量和数据库覆盖优先级。

## 9. 阅读顺序建议

如果你第一次接触这个后端，建议按这个顺序读：

1. [web.py](../../web.py)
2. [apis/article.py](../../apis/article.py)
3. [apis/mps.py](../../apis/mps.py)
4. [jobs/mps.py](../../jobs/mps.py)
5. [core/models](../../core/models/feed.py)
6. [core/config.py](../../core/config.py)

这样最容易把系统的主干看明白。
`,
  "docs/dev/architecture-overview": `# 系统架构概览

本文给一张总览图：WeRSS 分几层、数据怎样流动、新功能该落在哪。细节仍以各子模块文档为准。

## 1. 一句话架构

当前 WeRSS 可以概括成：

\`React 管理台 + FastAPI 接口层 + Python 业务核心 + 定时任务系统 + 微信采集与对象存储能力\`

它既不是「只爬网页」的脚本，也不是典型前后端分家的 SaaS，而是一套从采集、结构化、分析到分发的整体。

## 2. 系统分层

### 前端层

- 技术：React 18 + TypeScript + Vite
- 位置：\`web_ui/\`
- 职责：页面、路由、交互、调用后端 API

### 接口层

- 技术：FastAPI
- 位置：\`web.py\` + \`apis/\`
- 职责：HTTP 路由、认证、参数校验、统一响应

### 核心业务层

- 技术：Python 模块
- 位置：\`core/\`
- 职责：配置、模型、采集、标签提取、聚类、热点、通知、导出、对象存储

### 调度与后台任务层

- 技术：Python + \`TaskScheduler\` / APScheduler 思路
- 位置：\`jobs/\`
- 职责：定时采集、补全文、消息任务、文章清理、热点发现

### 资源与采集驱动层

- 技术：微信采集逻辑 + Playwright / 浏览器驱动 + 对象存储
- 位置：\`driver/\`、\`core/wx/\`、相关存储模块
- 职责：把外部内容拉进系统，并管理图片等资源

## 3. 目录到职责的映射

最重要的目录可以这样看：

- \`main.py\`
  进程入口和启动参数处理
- \`web.py\`
  FastAPI app 装配
- \`apis/\`
  路由和接口契约
- \`core/\`
  业务逻辑与模型
- \`jobs/\`
  定时任务
- \`driver/\`
  微信和浏览器驱动
- \`web_ui/\`
  React 管理台
- \`docs/\`
  项目文档

## 4. 一条主链路怎么流动

最核心的业务流可以简化为：

\`\`\`text
公众号来源
  -> 订阅记录
  -> 文章采集
  -> 文章入库
  -> 标签提取 / AI 过滤
  -> 标签聚类 / 热点发现
  -> RSS / 消息任务 / 导出
\`\`\`

这条链路解释了为什么项目会同时包含：

- 采集能力
- 标签能力
- 热点能力
- 推送能力

它们并不是互不相关的功能堆砌，而是同一条内容处理链上的不同环节。

## 5. 请求流和任务流要分开理解

### 请求流

用户操作页面时，大致路径是：

\`\`\`text
浏览器
  -> React 页面
  -> /api/*
  -> apis/*.py
  -> core/*
  -> 数据库 / 存储 / 外部服务
\`\`\`

### 任务流

后台自动运行时，大致路径是：

\`\`\`text
调度器
  -> jobs/*.py
  -> core/*
  -> 数据库 / 微信 / Webhook / 存储
\`\`\`

这两条流经常会落到同一个 \`core\` 能力上，但入口不一样。

## 6. 当前几个核心能力怎么落层

### 文章采集

- 页面入口：订阅管理、公众号管理
- 接口层：\`apis/mps.py\`
- 核心层：\`core/wx/\`
- 任务层：\`jobs/mps.py\`

### 文章工作台

- 页面入口：\`/articles\`
- 接口层：\`apis/article.py\`
- 核心层：文章模型、AI 过滤、标签提取等

### 标签与聚类

- 页面入口：\`/tags\`、\`/tag-clusters\`
- 接口层：\`apis/tags.py\`、\`apis/tag_clusters.py\`
- 核心层：\`core/tag_extractor.py\`、\`core/tag_cluster.py\`

### 热点发现

- 页面入口：\`/hot-topics\`
- 接口层：\`apis/hot_topics.py\`
- 核心层：\`core.hot_topics.*\`
- 任务层：\`jobs/hot_topics_job.py\`

### 消息任务

- 页面入口：\`/message-tasks\`
- 接口层：\`apis/message_task.py\`
- 任务层：\`jobs/taskmsg.py\`、\`jobs/mps.py\`
- 核心层：通知和模板渲染相关模块

## 7. 配置为什么是一个单独问题

这个项目的配置不是简单单源读取，而是多层覆盖：

- 示例配置文件
- 本地配置文件
- \`.env\`
- 数据库配置覆盖
- 运行时环境变量

所以配置是架构的一部分，而不是普通附属细节。

如果一个功能改动涉及开关、cron、模型名、存储策略，你就已经进入配置架构范围了。

## 8. 开发时该怎么定位改动层

### 改页面展示

先看 \`web_ui/\`

### 改接口行为

先看 \`apis/\`，再追到 \`core/\`

### 改后台调度

先看 \`jobs/\`

### 改数据结构

先看 \`core/models/\`

### 改运行配置

先看 \`core/config.py\` 以及配置相关文档

## 9. 这套架构最容易出的问题

- 路由层堆业务逻辑
- 同一能力在 API 和 job 里各写一份
- 配置改了但不清楚覆盖优先级
- 页面展示和真实接口返回长期漂移

所以整个文档系统才会强调一件事：

要始终以当前代码为准，而不是以过去的 README 或印象为准。

## 10. 建议继续阅读

如果你已经看懂这篇，继续读这些文档最有效：

- [backend-structure.md](./backend-structure.md)
- [frontend-structure.md](./frontend-structure.md)
- [jobs-and-schedulers.md](./jobs-and-schedulers.md)
- [data-model-overview.md](./data-model-overview.md)
- [workflows.md](../workflows.md)
`,
};